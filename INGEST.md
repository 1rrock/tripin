# Tripin — 콘텐츠 수집 파이프라인 핸드오프

> 자막 추출 → 장소 후보 분석 → 어드민 확정 자동화에 대한 결정 사항.
> **이 문서는 나중에 구현할 때 다시 조사하지 않기 위한 인수인계 메모다.**
> 작성: 2026-08-03 · 상태: **구현 완료 (2026-08-04)** — 사용법은 바로 아래 ★ 섹션
> 선행 문서: `CONCEPT.md`(제품) · `LEGAL.md`(법적 제약)

---

## ★ 구현 완료 — 새 채널 등록 사용법 (2026-08-04)

파이프라인은 Claude Code **스킬 `tripin-ingest`** 로 구현됐다
(`.claude/skills/tripin-ingest/SKILL.md` + `scripts/ingest/*.mjs`).
첫 실전: 추성훈 채널 16편 → 장소 20곳 (2026-08-04).

### Orca 신작 자동 동기화 (크론 대신)

- 채널 목록: `docs/channels.md` (`enabled: yes` 만)
- 자동화 프롬프트: `docs/ORCA_CHANNEL_SYNC_PROMPT.md` 복붙 → Orca 스케줄
- 동작: MD 읽기 → 채널 최근 영상 → DB에 없는 것만 → 자막 → 장소 → **candidate**
- 확정·요약·공개는 여전히 사람 (`/admin/confirm`, `/admin/place`)

### 새 채널 등록 절차

```
1. [수동] /admin/confirm → "+ 크리에이터"로 채널 생성
   (슬러그·이니셜·컬러는 사람이 정한다 — 자동 생성 금지)
   → docs/channels.md 표에도 행 추가 (enabled: yes)

2. [입력] Claude Code 세션에 아래 중 하나를 준다:
   · 채널 영상탭 InnerTube JSON 덤프 붙여넣기
     (유튜브 채널 영상탭 → 개발자도구 Network → "browse" 응답 복사)
   · 영상 URL 목록 ("이 영상들 등록해줘 https://...")
   · "추성훈처럼 ○○ 채널 해줘" → Claude가 덤프 붙여넣기를 요청함

3. [자동] Claude가 tripin-ingest 스킬로 진행:
   덤프 파싱 → 여행/맛집 영상 필터 → 자막 추출(로컬, IOS 클라이언트)
   → 장소 식별 → 웹검색으로 상호·주소·좌표·구글 등록 확인
   → 필요한 도시 생성(ensure-cities) → 영상+장소 후보 등록(insert-candidates)
   → 좌표 자동 백필(backfill-coords: 구글 공유링크 핀 > GSI 주소 > Nominatim)

4. [수동] /admin/confirm → 후보 검수 → 수정 → 확정
   · 구글 공유 링크만 붙여넣으면 좌표는 저장 시 자동 해석된다
   · sourceNote의 근거(자막 표기·검증 출처)를 보고 동명이점 판별

5. [수동] /admin/place → 요약 작성 (템플릿 + 금지어/자막복붙 검사)
   · mentionNote는 자막 파생물 — 참고만 하고 요약은 새로 쓴다
```

### 스크립트 (전부 로컬 실행 전용)

| 스크립트 | 역할 |
|----------|------|
| `scripts/ingest/parse-innertube.mjs <dump.json>` | InnerTube 덤프 → 영상 목록 |
| `scripts/ingest/fetch-transcript.mjs <id...>` | 자막 추출 (2.5초 딜레이, 원문 미저장) |
| `scripts/ingest/ensure-cities.mjs <cities.json>` | 없는 도시 생성 (중복 스킵) |
| `scripts/ingest/insert-candidates.mjs <payload.json>` | 영상+장소 후보 등록 (재실행 안전) |
| `scripts/ingest/backfill-coords.mjs [--dry]` | 좌표 없는 장소 좌표 자동 채움 |

### 알려진 제약 (2026-08 기준)

- **자막**: 웹 클라이언트 timedtext는 pot 토큰 요구로 빈 응답 → InnerTube **IOS 클라이언트**로 우회 중.
  막히면 `fetch-transcript.mjs` 상단 클라이언트 버전만 갱신
- **네이버 place ID**: 자동 검색이 ncaptcha에 걸림(우회 금지) → 확정 화면에서 URL 붙여넣으면 ID 자동 추출
- **카카오**: 해외 장소 미커버 — 일본/미국 장소는 대부분 null이 정상
- **가게명 검색 자동화**: `GOOGLE_PLACES_API_KEY`(서버용)가 비어 있음. 채우면 공유 링크 없이
  상호만으로 좌표+가게 페이지 링크 확보 가능 (브라우저용 MAPS 키는 리퍼러 제한이라 서버에서 못 씀)
- 상호가 자막·검색으로 특정 안 되면 **추측 등록 금지** — unidentified로 보고만 하고 사람이 찾는다

---

## 0. 지금 이걸 안 만드는 이유

서비스(프론트 + 데이터 모델)를 먼저 만든다. 이유:

- 파이프라인은 **어드민이 있어야 의미가 있다.** 후보를 뽑아도 확정할 화면이 없으면 쓸모없음
- Batch 0(1채널 × 1도시, 12핀)은 **손으로 해도 2시간**이면 된다. 자동화 없이 검증 가능
- 실제로 손으로 12핀을 만들어봐야 어드민에 뭐가 필요한지 알게 된다

**따라서 순서**: 서비스 껍데기 → 손으로 Batch 0 → 어드민 → 그다음 이 문서의 파이프라인.

---

## 1. 전체 흐름

```
① 채널 → 영상 목록          YouTube Data API
② 여행/맛집 영상 필터        제목·설명 + LLM 판정
③ 자막 추출                 로컬 라이브러리 (원문 미저장)
④ 자막 → 장소 후보 분석      LLM
⑤ 후보 → 지도 검색 매칭      오픈 데이터셋
⑥ 어드민에서 사람이 확정      ← 여기만 수동
```

**①~⑤ 자동, ⑥만 수동.** ⑥의 수동 범위도 "동명이점 판별" 하나로 좁힌다(3.3 참조).

---

## 2. 단계별 구현 메모

### ① 채널 → 영상 목록

```
channels.list(part=contentDetails, id=채널ID)
  → contentDetails.relatedPlaylists.uploads  (업로드 재생목록 ID)
playlistItems.list(part=snippet, playlistId=위 ID, maxResults=50)
  → 영상 ID·제목·게시일·썸네일
```

**⚠️ `search.list` 절대 쓰지 말 것 — 100 units다.**
기본 할당량 10,000/day 기준으로 하루 100회밖에 못 쓴다.
`playlistItems.list`·`videos.list`·`channels.list`는 전부 **1 unit**. (근거: `LEGAL.md` 4.5)

**저장 제약**: 영상 메타(제목·게시일·썸네일)는 YouTube API 정책상 **30일 초과 보관 금지**.
`Video.apiFetchedAt` 필드를 두고 월 1회 갱신 배치로 리셋한다. 이건 선택이 아니라 정책 요건이다.

### ② 여행/맛집 영상 필터

- 1차: 제목·설명에 도시명/국가명/음식 키워드
- 2차: 애매하면 LLM에 제목+설명 넘겨 판정
- **쇼츠 제외** — 자막이 짧아 상호 누락이 많다 (README 6.3에서 확인된 사실)

### ③ 자막 추출 ★ 유일한 제약 구간

**공식 API로는 불가능하다.** `captions.download`는 **본인 소유 영상만** 받을 수 있다.

**채택: 로컬 라이브러리**
- `youtube-transcript-api` (Python) 또는 `yt-dlp --write-auto-sub --skip-download`
- ❌ downsub.com 등 웹 서비스는 **파이프라인 의존성으로 쓰지 않는다**
  - 남의 서버라 마크업 변경·레이트리밋·서비스 종료 시 파이프라인이 멈춤
  - 어드민에 통합 불가(중간에 사람이 껴야 함)
  - 손으로 한두 개 테스트할 때만 사용

**실행 규칙 (차단 격리)**
| 규칙 | 이유 |
|------|------|
| **로컬(어드민 머신)에서만 실행** | 서비스 서버 IP와 분리 → 차단당해도 Tripin 본체는 무사 |
| 요청 간 2~3초 딜레이 | 조각당 영상 4개면 체감 차이 없음 |
| 하루 처리량 상한 (채널 1개씩 배치) | 실제 볼륨은 하루 1~2개 수준이라 여유 |
| 실패 시 재시도 안 함 | 다음 배치로 넘김 |

> 실제 볼륨: 조각당 영상 4개 × 월 8~12조각 = **월 40~50개, 하루 1~2개.**
> 사람이 브라우저로 여는 것보다 적다. 이 규모에서 차단은 현실적 우려가 아니다.

**⛔ 절대 규칙 — 자막 원문은 저장하지 않는다**
- 메모리 또는 임시 파일에서만 처리, 완료 즉시 폐기
- DB 스키마에 `transcriptFull`, `rawCaptionBlob`, `captionSegments` 등 **필드를 만들지 않는다**
- 어드민 화면에 띄우는 건 가능(작업용), 저장은 안 됨
- 근거: `LEGAL.md` 3.3 — 이게 무너지면 방어선 전체가 무너진다

### ④ 자막 → 장소 후보 분석 (LLM)

**추출 대상**
```
상호명 (원어 + 한글 표기)
지역 힌트 (구/역/랜드마크)
업종 (식당/카페/명소/숙소/뷰포인트)
타임스탬프 (자막 큐 시작 초)
맥락 메모 (짧게)
확신도 (high/medium/low)
```

**프롬프트 원칙**
- "자막을 요약하라"가 아니라 **"자막에서 장소 관련 사실만 추출하라"**
- 대사·표현을 그대로 옮기지 말 것 → 이후 요약문 작성 시 복붙 방지
- 상호가 불명확하면 억지로 만들지 말고 `null` + `confidence: low`
- `confidence: low`는 어드민에만 표시하고 공개 후보에서 제외

**검증된 사례**: 추성훈 채널 도쿄 돈카츠 편에서 `돈카츠 마루시치` / `도쿄 고토구` / 가격·웨이팅 힌트 추출 성공 (README 6.3)

### ⑤ 후보 → 지도 검색 매칭

**데이터 소스: Overture Maps Places (CDLA Permissive 2.0) 또는 Foursquare OS Places (Apache 2.0)**

⚠️ **Google Places를 쓰면 안 된다.** MapLibre 지도와 조합 시 약관 위반이다.
> "Customer will not (i) display or use Places content on a non-Google Map"

| 소스 | 캐싱 가능? |
|------|-----------|
| Google Places | `place_id`만 (딥링크 전용으로만 사용) |
| Kakao Local | ❌ 전면 불허, 라이브 호출만 → SSG와 충돌 |
| **Overture / Foursquare** | ✅ 자유 — 라이선스 데이터셋 |

상세: `LEGAL.md` 4장

**출력**: 후보당 지도 검색 결과 **3~5개**를 어드민에 카드로 전달

### ⑥ 어드민 확정 — 3.3 참조

---

## 3. 어드민 사양 (파이프라인의 소비자)

### 3.1 화면

```
[URL 입력]  또는  [채널 선택 → 미처리 영상 목록]
        ↓
[처리 중] 자막 추출 → 장소 분석 → 지도 매칭
        ↓
┌─ 후보 카드 ────────────────────────┐
│ 돈카츠 마루시치  (とんかつ まるしち)   │
│ 자막 12:40 · 확신도 high             │
│ "고토구, 웨이팅 있음, 로스카츠 2천엔대"│
│                                    │
│ 지도 검색 결과:                      │
│  ① 마루시치 스미요시점  고토구 ...    │
│  ② 마루시치 긴자점     주오구 ...    │
│  ③ 마루시치 본점       ...          │
│                                    │
│ [1][2][3] 확정  [C] 미확인  [X] 버림 │
└────────────────────────────────────┘
```

### 3.2 속도 목표

**확정 1건당 10초.** 이 숫자가 콘텐츠 생산 속도이자 사업성을 결정한다.
- 키보드 단축키 필수 (`1`/`2`/`3` 확정, `C` candidate, `X` 버림, `Esc` 보류)
- 마우스 없이 연속 처리 가능해야 함
- 12핀이면 2분 안에 끝나야 정상

### 3.3 ★ 자동 확정 규칙 — 오확정 방지

**이 프로젝트 유일한 구조적 리스크가 동명이점 오확정이다.**
엉뚱한 지점을 "OO 유튜버가 간 곳"으로 표시하면 그 가게는 오지 않은 손님을 받고, 진짜 가게는 트래픽을 잃는다. **사실이 아닌 내용의 적시**라 "객관적 사실 부합" 방어가 작동하지 않는다.

```
지도 검색 결과가 1개 + 확신도 high   → 자동 확정 허용
지도 검색 결과가 2개 이상            → candidate 강제, 사람이 볼 때까지 비공개
체인·동명이점으로 판별됨              → 지점 특정 전까지 확정 버튼 비활성
```

**확정 시 `sourceNote`에 근거 입력 강제** — 영상 속 간판 / 지역 언급 / 타임스탬프 중 하나.
근거 없이 확정 못 하게 막는다. (근거: `LEGAL.md` 4.6)

### 3.4 데이터 계약

파이프라인 출력 → 어드민 입력:

```json
{
  "videoId": "VVWn_5AxJSk",
  "candidates": [
    {
      "name": "돈카츠 마루시치",
      "nameLocal": "とんかつ まるしち",
      "regionHint": "도쿄 고토구",
      "placeType": "restaurant",
      "timestampSec": 760,
      "mentionNote": "워뇨타이 추천으로 소개",
      "confidence": "high",
      "mapMatches": [
        { "sourceId": "...", "name": "...", "address": "...", "lat": 0, "lng": 0 }
      ]
    }
  ]
}
```

확정 시 `Place` + `VideoPlace` 레코드 생성. 스키마는 `CONCEPT.md` 6장.

---

## 4. 자동화되지 않는 것 (기대치 조정)

파이프라인이 끝나도 **요약문 작성은 사람이 한다.**

- LLM이 초안을 뽑을 수는 있지만 그대로 쓰면 AdSense "스크랩된 콘텐츠" 판정 위험
- Google 정책의 탈출구가 문자 그대로 *"additional commentary, curation, or otherwise adding value"* 다
- 요약 미작성은 YouTube API §III.G.1.d(광고 게재 시 독립적 가치 요건)에도 걸린다 → **요약은 법적 요건**

**따라서 조각 하나의 실제 시간 구성**
| 단계 | 자동화 후 |
|------|-----------|
| ①~⑤ 파이프라인 | ~5분 (대기) |
| ⑥ 어드민 확정 | ~3분 (12핀 × 10초 + 여유) |
| **요약 작성** | **~40분 ← 남는 병목** |
| 인트로·검수 | ~15분 |

자동화의 실익은 "확정 30분 → 3분". 요약 40분은 그대로 남는다.

---

## 5. 구현 순서 (나중에)

1. `③ 자막 추출` 단독 스크립트 — 영상 1개로 동작 확인
2. `④ LLM 분석` 붙이기 — 추성훈 도쿄 편으로 회귀 테스트 (기지의 정답이 있음)
3. `⑤ 지도 매칭` — **여기서 오픈 데이터셋 매칭률 실측** ← 중요
4. `①② 채널 순회` — 배치화
5. 어드민 확정 UI 연결

**3번이 분기점이다.** 도쿄·방콕 골목 식당이 Overture/Foursquare에 없으면 B안이 무너지고, Google Maps JS API + Places(A안) 비용을 받아들여야 한다. Batch 0의 12핀으로 자연스럽게 검증된다.

---

## 6. 체크리스트

**구현 시 반드시**
- [ ] `playlistItems.list` 사용, `search.list` 금지
- [ ] 자막 추출은 로컬 실행, 딜레이 2~3초
- [ ] 자막 원문 미저장 — DB 필드 자체를 만들지 않음
- [ ] 장소 데이터는 Overture/Foursquare, Google Places는 `place_id` 딥링크 전용
- [ ] 후보 2개 이상이면 candidate 강제
- [ ] 확정 시 `sourceNote` 근거 입력 강제
- [x] `Video.apiFetchedAt` + 월 1회 갱신 배치 — `src/app/api/cron/refresh-youtube-meta/route.ts`
      (스케줄: `vercel.json`, 매월 1일 03:00 UTC. `YOUTUBE_API_KEY` + `CRON_SECRET` 필요)
      `creators.display_name`/`avatar_url` 도 같은 배치가 함께 갱신한다
- [ ] `confidence: low`는 공개 후보에서 제외

**절대 안 됨**
- [ ] ❌ 자막 원문 DB 저장·화면 게시
- [ ] ❌ LLM 요약 그대로 공개 (사람 검수 없이)
- [ ] ❌ 서비스 서버에서 자막 추출 실행
- [ ] ❌ 후보 1개 아닌데 자동 확정

---

## 7. 미해결

| 항목 | 비고 |
|------|------|
| 오픈 데이터셋 해외 골목 식당 매칭률 | 구현 5-③에서 실측 |
| YouTube API §III.E.2 교차집계 조항 | 무관한 채널 집계가 걸리는지 불명확. 확대 전 API 지원팀 서면 문의 (`LEGAL.md` 7장) |
| `videoId` 30일 면제 여부 | 명시적 예외 못 찾음. 보수적으로 전체 갱신 |
| LLM 프롬프트 세부 | 실제 자막으로 튜닝 필요 |
