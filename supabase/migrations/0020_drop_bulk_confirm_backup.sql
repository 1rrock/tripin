-- ═══════════════════════════════════════════════════════
-- ⛔ 적용 전 사람 확인 필요 — 2026-08-05 롤백 재료를 영구 삭제한다. ⛔
--
-- 되돌릴 수 없다. 이 파일을 적용하면 `_bulk_confirm_20260805` 12행이 사라지고,
-- 그 안에 있던 "일괄 확정 직전의 map_status" 를 재구성할 방법이 없어진다.
-- 즉 0002_confirm_all_candidates.sql 이 적어 둔 롤백 절차
--     update places set map_status = 'candidate' where id in (...);
-- 의 `(...)` 를 채울 근거가 없어진다.
--
-- 0002 는 "이건 검수를 거친 확정이 아니다" 라고 명시하고, 공개 배포 전에 사람이
-- 다시 볼 항목을 열거해 뒀다(그 파일 7-13행): 매드해피 도쿄 · 다케시타 거리 ·
-- 이마카츠 롯폰기 본점 · 쿠시카츠 다나카 아메리카무라점 · 나가하마 넘버원 기온점 ·
-- 돈키호테 후쿠오카 텐진 본점. 동명이점 오확정은 이 프로젝트의 유일한 구조적
-- 리스크다(0001_init.sql:121, LEGAL.md 4.6).
--
-- ⇒ 그 검수가 끝났고 되돌릴 일이 없다고 **사람이 판단한 뒤에만** 적용하라.
--
-- 0018·0019 와 일부러 분리한 파일이다. 그 둘은 아무 때나 안전하게 적용 가능해야
-- 하고, 이건 그렇지 않다. `npm run db:migrate` 는 미적용 파일을 **전부** 순서대로
-- 돌리므로, 아직 지울 때가 아니라면 이 파일을 워킹 트리에서 빼 두거나
-- `_migrations` 에 이름만 미리 넣어 건너뛰게 하라:
--     insert into _migrations(name) values ('0020_drop_bulk_confirm_backup.sql');
--
-- 지우기 전에 사본을 뜨고 싶다면:
--     \copy (select * from _bulk_confirm_20260805) to 'bulk_confirm_20260805.csv' csv header
-- ═══════════════════════════════════════════════════════

drop table if exists public._bulk_confirm_20260805;
