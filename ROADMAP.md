# Roadmap

계정 · 저장 · 유저 참여 · 수익화의 순서와, 각 단계를 여는 조건.

`PRODUCT.md` 가 "무엇을 만드는가" 라면 이 문서는 **"무엇을 언제 여는가"** 다.

---

## 결정 원칙 — 되돌릴 수 있는가

이 문서의 모든 순서는 하나의 비대칭에서 나온다.

| | 미루면 | 그래서 |
|---|---|---|
| **계정** | 비용이 **커진다**. 앱 출시 후 도입하면 데이터 마이그레이션 + 유저 재가입 | **지금 한다** |
| **커뮤니티** | 비용이 안 커진다. 대신 열면 **못 닫는다** (법적 의무 + 유저 글 삭제 = 신뢰 파괴) | **최대한 미룬다** |

"나중에 하면 또 일이잖아" 는 계정에는 맞고, 커뮤니티에는 정반대로 적용된다.

---

## 확정 사항

### 계정: 만든다. 단, "회원가입"은 만들지 않는다.

필요한 것은 **서버 저장**이지 가입 폼이 아니다.

1. 하트를 처음 누르는 순간 백그라운드로 `signInAnonymously()`. 화면에 아무것도 뜨지 않는다.
   → `PRODUCT.md` 의 "게이트는 없다"(원칙 5)가 그대로 지켜진다.
2. 저장은 처음부터 서버로. 인앱 웹뷰·ITP·기기 변경과 무관해진다.
3. 로그인은 **기기를 넘을 때만** 요구. 익명 계정을 승격시켜 데이터를 그대로 승계.
4. 앱은 같은 인증 체계를 재사용.

#### 확정된 설계 (2026-08-14)

| 항목 | 결정 |
|---|---|
| **저장 흐름** | 하트 클릭 → **즉시 저장**, 로그인 창 없음. 익명 계정이 백그라운드로 생성된다 |
| **로그인 수단** | **구글 하나만.** 앱 출시 시점에 Sign in with Apple 추가 (4.8 강제) |
| **로그인 권유 위치** | **`/saved` 목록 화면 상단 배너에서만.** 홈·지도·장소 상세에는 절대 노출하지 않는다 |
| **승격** | 구글 로그인 시 익명 계정의 저장·구독을 전부 승계. 다시 모으게 하지 않는다 |

배너 문구 방향: "이 기기에서만 볼 수 있어요 → 연결하기"
가치를 이미 받은 사람에게만 보이므로 게이트가 아니다.

비로그인으로 끝까지 되는 것: 검색 · 지도 · 도시/채널/영상/장소 상세 · 언어 전환 — 즉 **저장·구독을 뺀 전부**.

**로컬 저장(localStorage)은 채택하지 않는다.** 앱 출시와 인스타/쇼츠 유입 계획 때문:
- iOS 인스타 인앱 웹뷰(WKWebView)는 저장소가 앱 샌드박스에 격리된다. 거기서 저장하고 Safari로 열면 **같은 기기인데도 없다.**
- Safari ITP는 7일 미접속 시 스크립트로 쓴 저장소를 지운다. "모아뒀다가 한 달 뒤 출발"이 이 제품의 사용 주기인데 정확히 그 구간이 깨진다.
- 앱 설치 시 웹 저장분을 승계할 방법이 없다.

### 커뮤니티(게시판·댓글·리뷰): 하지 않는다.

**수익 경로가 아니다.** 계획된 수익 모델 어디에도 안 들어간다:

| 수익 모델 | 커뮤니티 필요? | 실제로 필요한 것 |
|---|---|---|
| AdSense (이미 계획됨 — `src/app/(public)/privacy/page.tsx:88`) | ❌ | 페이지 수 × 체류 × 독립적 가치 |
| 여행 제휴 (숙소·액티비티) | ❌ | 저장 목록 + 위치 데이터 |
| 유료 구독 | ❌ | 팔 것이 없다. 지도로 내보내는 것이 본업 |
| 가게 B2B | ❌ | 아직 규모가 아니다 |

오히려 **해가 된다.** 글 없는 빈 게시판은 저품질 페이지 양산이라 AdSense '독립적 가치' 요건(`supabase/migrations/0001_init.sql:116` 주석)에 불리하다.

**법적 부담이 결정적이다.** 스키마는 이미 정보통신망법 의무를 안고 설계돼 있다:

```sql
-- 0001_init.sql:31 — 채널 단위 즉시 비공개 스위치 역할. 정보통신망법 제44조의3 근거
-- 0001_init.sql:172 — 임시조치는 30일 이내에 결론
-- 0001_init.sql:185
blinded_at  timestamptz,   -- 임시조치 시작 시각. +30일이 결론 기한.
```

지금은 대상이 "우리가 올린 장소·영상"뿐이라 1인 운영으로 감당된다.
커뮤니티를 열면 이 의무가 **유저 글 전부**로 확대된다. 가게 대상 부정 리뷰 → 게시중단 요청 → 30일 내 결론. 한국은 사실을 적어도 명예훼손이 성립한다.

### 유저 참여: 한다. 글쓰기가 아닌 형태로.

이 제품의 병목은 **확정 장소 수**(`PRODUCT.md` — "성공은 확정된 장소가 실제로 몇 곳 쌓였는가")이고, 그건 운영자 수작업이다. 유저 참여를 붙일 자리는 여기다.

- "여기 문 닫았어요" 신고
- "이 장소 맞아요 / 아니에요" — 오확정은 프로젝트의 **유일한 구조적 리스크**(`0001_init.sql:121` 주석)
- "이 영상에 이 가게" 제보

전부 **폼이지 게시판이 아니다.** 공개 표시가 없으니 명예훼손·임시조치·모더레이션이 발생하지 않는데, 핵심 지표는 직접 올라간다.

패턴은 이미 검증돼 있다 — `takedown_requests` 의 "누구나 INSERT, 아무도 SELECT" 정책(`0006_close_takedown_insert.sql`)을 그대로 재사용한다.

**이미 작동 중인 유저 참여도 있다.** `search_misses`(`0001_init.sql:193`):

> 주석 그대로: *"다음에 만들 조각의 우선순위를 정하는 최고의 데이터"*

유저는 아무것도 쓰지 않고 검색만 해도 다음에 확정할 것을 알려준다. 어드민 화면(`src/app/admin/search-misses/`)도 이미 있다.

---

## 스키마를 미리 열어둔다 — 기능은 0개

커뮤니티를 나중에 정말 열게 될 때 마이그레이션 한 번으로 끝나도록, **인증 설계 시점에** 다음 3가지를 잡아둔다. 확장 비용의 대부분이 선불로 해결되고, 지금 만드는 기능은 늘지 않는다.

1. **`profiles`** — 익명 유저도 row 생성. `display_name`, `avatar_url` 은 nullable로 두고 쓰지 않는다. 나중에 작성자 표시가 필요해지면 컬럼 추가가 아니라 값 채우기로 끝난다.
2. **저장 테이블의 `visibility`** — `private` 고정으로 시작. "공개 목록 공유"가 나중에 여기서 나온다.
3. **제보/신고는 별도 테이블** — `takedown_requests` 에 섞지 않는다. 성격이 다르고, 나중에 공개될 수 있는 쪽은 이쪽이다.

---

## 단계

| # | 단계 | 내용 | 왜 이 순서 | 다음으로 넘어가는 트리거 |
|---|---|---|---|---|
| 0 | **인증 토대** | `@supabase/ssr` 도입, 익명 인증, `profiles`/`saved_places`, RLS `auth.uid()` 정책 | 뒤의 전부가 여기 얹힌다 | 완료 시 |
| 1 | **저장 기능** | 하트 · 갔던 곳 · `/saved` | 리텐션이자 제휴의 원재료 | 저장 발생 세션 비율 관측 |
| 2 | **처리방침 개정** | 수집항목 · 보관기간 · 파기 · 해외이전(Supabase) | 로그인이 붙는 순간 **법적 필수** | 1과 동시 |
| 3 | **크라우드 검증** | 폐업 · 오확정 신고 폼 | 핵심 지표를 직접 가속 | 신고 유입량 |
| 4 | **AdSense** | 광고 게재 | 페이지·체류가 먼저 쌓여야 승인된다 | 페이지 수 · 트래픽 |
| 5 | **앱 + 딥링크** | 웹 저장분 이어받기. 여기서 Apple $99 결제 | 0번 인증 없이는 시작 불가. 4.2 통과할 네이티브 가치도 필요 | 웹 저장 유저 확보 |
| 6 | **제휴 수익** | 저장 목록 기반 숙소 · 액티비티 | 저장 데이터가 쌓여야 의미 | 저장 목록 밀도 |
| 7 | **커뮤니티** | ⚠️ **조건부 — 아래 참조** | — | — |

### 0단계 착수 시 주의

- **별도 브랜치로 간다.** 현행 `design-v3` 는 디자인 개편 중이고 i18n 사전 키 누락 타입 에러가 다수 있다. 인증은 데이터 계층 변경이라 UI 리팩터링과 섞이면 되돌리기 어렵다.
- 추가할 패키지는 `@supabase/ssr` **하나뿐**이다. `signInAnonymously` 는 설치된 `@supabase/auth-js@2.112.0` 에 이미 있다.
- 공개 화면 대부분이 이미 `"use client"`(`PlaceSheet`, `ResultRow`, `Thumb`, `MapView`)라 하트 버튼을 위한 서버→클라이언트 전환 리팩터링은 없다.

---

## 앱 출시

### 비용 — 지금은 0원이다

| 항목 | 비용 | 언제 필요 |
|---|---|---|
| Apple Developer Program | **$99 / 년** | iOS 앱 출시 시. 웹에서 Sign in with Apple 을 쓸 때도 Services ID 때문에 필요 |
| Google Play Console | $25 (1회) | 안드로이드 출시 시 |
| 구글 로그인 | 무료 | 지금 가능 |

**가이드라인 4.8은 앱에만 적용된다.** 웹사이트는 앱스토어 심사 대상이 아니다.
→ **웹 단계에서는 구글 로그인만 넣고 $99 는 앱 낼 때 낸다.**

### Sign in with Apple 은 회피 가능하다

4.8 면제 조항:

> Sign in with Apple is not required if your app **exclusively uses your company's own account setup and sign-in systems**

익명 인증 + 자체 이메일 로그인만 쓰면 **애플 로그인이 아예 불필요하다.**
**구글 로그인을 넣는 순간** 애플 로그인이 강제된다. 편의성과 맞바꾸는 선택지다.

⚠️ **한국 개발자 전용 요건** — 2026-01-01부터 한국 소재 개발자는 Services ID 등록·갱신 시 **server-to-server notification endpoint** 를 제공해야 한다. 애플 로그인을 붙이기로 하면 엔드포인트 구현이 추가된다.

### 지금 출시하지 않는 이유

1. **앱은 획득 채널이 아니다.** 앱스토어 검색 유입은 거의 없다. 앱은 이미 있는 유저를 붙잡는 도구다.
2. **가이드라인 4.2 (Minimum Functionality)** — 웹을 그대로 감싼 앱은 리젝된다. 통과하려면 네이티브 가치가 실제로 있어야 한다:
   - 여행지 오프라인에서 저장 목록 열람
   - 위치 기반 "지금 근처 확정 장소"
   - 구독 채널 새 영상 푸시

   이 셋은 이 제품에서 자연스럽게 나오지만, **재료(저장·구독 데이터)가 먼저 쌓여야** 만들 수 있다.

### 구글 클라우드 — `eatripin` 프로젝트 (2026-08-14 생성)

계정: `minddoni0703@gmail.com` (Supabase tripin 프로젝트와 동일 계정).

- OAuth 동의 화면: 앱 이름 **Eatripin**, 대상 **외부**
- OAuth 클라이언트: `Eatripin Web (Supabase)` (웹 애플리케이션)
- 리디렉션 URI: `https://cdimzihwqkesvcmogdkd.supabase.co/auth/v1/callback`

**optisearch 에 만들지 않은 이유:** OAuth 동의 화면은 GCP 프로젝트당 하나다. optisearch 에 만들면 Eatripin 유저가 구글 로그인할 때 "optisearch에 로그인" 이 뜬다. 로그인은 신뢰가 걸린 유일한 순간이라 여기서 낯선 이름이 뜨면 그대로 이탈한다.

**⚠️ 앱이 아직 "테스트" 모드다.** 외부 대상은 게시 전까지 테스트 사용자만 로그인할 수 있다. 실제 유저에게 열려면 `대상 → 앱 게시`. 요청 스코프가 email·profile·openid 뿐이라 **구글 검증 심사는 필요 없다** (민감 스코프 아님).

#### 🔴 남은 대시보드 작업 — 이게 안 돼 있어서 로그인이 안 됐다 (2026-08-14 확인)

GCP 쪽은 다 돼 있는데 **Supabase 쪽에 구글을 안 붙였다.** 실제 응답:

```
GET /auth/v1/user/identities/authorize?provider=google
→ 400 {"error_code":"validation_failed",
       "msg":"Unsupported provider: provider is not enabled"}
```

같은 요청에서 익명 가입(`POST /auth/v1/signup`)은 200 이므로 **익명 인증만 켜져 있는 상태**다.
증상이 "눌러도 아무 일도 안 남" 하나로만 보여서 원인을 가리기 어려웠다.

대시보드를 직접 훑어 항목별로 확인·조치한 결과 — **넷 다 완료. 로그인 동작 확인함**(2026-08-15):

| # | 위치 | 항목 | 상태 |
|---|---|---|---|
| 1 | Supabase · Auth → Providers | **Google 활성화** + Client ID·Secret | ✅ 설정함 |
| 2 | Supabase · Auth → Providers · User Signups | Allow manual linking | ✅ 이미 켜져 있었다 |
| 3 | Supabase · Auth → URL Configuration | Redirect URLs | ✅ `http://localhost:3000/**` 추가함 |
| 4 | GCP · 대상 | 테스트 사용자 | ✅ `minddoni0703@gmail.com` 추가함 |

1번의 Client ID(공개값 — OAuth URL 에 그대로 실려 나간다). 재설정하거나 다른 환경을 붙일 때 쓴다:

```
226117802795-2dmvtbunb8amblkd32i61lelb06q6460.apps.googleusercontent.com
```

Client Secret 은 GCP 콘솔의 같은 클라이언트 상세(`클라이언트 → Eatripin Web (Supabase)` → 추가 정보)에 있다.

**2번은 켜져 있어서 다행이다.** 기본값이 꺼짐이라 흔히 놓치는데, 1번만 켜고 2번이 꺼져 있으면
**저장한 적 없는 사람은 로그인되고 저장한 사람은 안 되는** 갈린 증상이 나온다
(후자는 익명 계정 승격이라 `linkIdentity` 경로를 탄다).

⚠️ **앱은 계속 "테스트 중"이다.** 지금은 테스트 사용자 1명만 로그인할 수 있다.
`대상 → 앱 게시` 는 **배포 시점에** 한다 — Site URL 이 아직 `http://localhost:3000` 이라
지금 공개해 봐야 열어줄 곳이 없다. 게시할 때 Redirect URLs 에 배포 도메인 `/**` 도 같이 추가한다.

### 🔜 Maps API 를 `eatripin` 프로젝트로 이전

지금은 optisearch 프로젝트에 있다. 옮기는 것이 맞지만 **별도 작업**으로 잡는다 — OAuth 와 달리 Maps 는 **결제 계정 등록이 선행**돼야 하고, 프로덕션 지도가 걸려 있다.

순서를 지켜야 한다:
1. `eatripin` 에 결제 계정 등록
2. 새 키 발급 + 제한 설정 — API 제한 **Maps JavaScript API 하나만**, HTTP 리퍼러(`localhost:3000` + 배포 도메인) (`shared/config/env.ts:41`)
3. `.env.local` **과 Vercel 환경변수** 양쪽 교체
4. 실제 지도 렌더 확인
5. 그제서야 optisearch 쪽 구키 삭제

`GOOGLE_PLACES_API_KEY`(서버 키)도 따로 옮긴다. `env.ts:43` — **브라우저 키와 절대 같은 키를 쓰지 않는다.**

미루면 오늘 두 번 겪은 "계정이 달라 콘솔에 못 들어감" 문제가 지도에도 남는다.

### 그 외 제출물

- **App Privacy 라벨 / Play Data safety** — 계정이 생기는 순간 둘 다 제출물이 된다.
- **딥링크** — 쇼츠 → 웹 저장 → 앱 설치 후 승계. 익명 계정이 있어야 이 연결이 성립한다.

---

## 앱인토스(토스 미니앱) — 첫 출시처로 쓰지 않는다

[서비스 오픈 정책](https://developers-apps-in-toss.toss.im/intro/guide) 확인 결과, 이 제품과 충돌하는 항목이 많다.

| 정책 | 문서 원문 | 영향 |
|---|---|---|
| **광고** | "앱인토스 전면형·보상형·배너 광고만. 외부 광고 네트워크를 통한 광고 연동은 허용되지 않아요" | 🔴 **AdSense 봉쇄.** 4단계 수익 모델이 통째로 막힌다 |
| **로그인** | "미니앱 내 로그인은 토스 로그인만 사용할 수 있어요. 그 외 로그인 기능 및 소셜, 간편 로그인은 사용할 수 없어요" | 🔴 익명+구글 인증을 못 쓴다 → **인증 이원화** |
| **외부 링크** | "미니앱 내에서 '앱 내 기능'을 완결적으로 제공하지 않는 구조" 금지 / 단 "단순 정보 확인을 위한 타사 웹사이트 이동"은 허용 | 🟡 **최대 리스크.** 본업이 유튜브·지도 아웃링크(`PRODUCT.md` 원칙 4)다. **착수 전 토스에 사전 문의 필요** |
| **앱 설치 유도** | "앱을 설치하시면 더 많은 기능을…" 문구·배너 금지 | 🟡 자사 앱 유입 경로로 못 쓴다 |
| **사업자등록** | 인앱광고·인앱결제·토스페이·프로모션·비즈월렛·**토스 로그인** 사용 시 필수. 사업자등록증 업종과 미니앱 서비스 업종이 **일치해야 함** | 🟡 로그인이 있는 미니앱이면 사업자등록 필수. 검토 1~2 영업일 |

**콘텐츠 자체는 문제없다.** 금지 업종 8가지(가상자산·자금세탁·불법조장·사행성·금융·투자자문·의료·내부정책)에 해당하지 않는다.

**결론:** AdSense 봉쇄 하나만으로도 첫 출시처로는 부적합하다. 수익 모델을 바꾸고 인증을 따로 만들어야 한다.
나중에 **제3의 유통 채널**로는 검토 가치가 있다 — 토스 3천만 유저 노출은 실제로 크다. 그때는 외부 링크 정책 사전 문의가 선행 조건이다.

기술 참고: 기존 웹 프로젝트에 `@apps-in-toss/web-framework` 를 설치해 전환하는 경로가 있으나, 공식 문서는 Vite 기준이고 **Next.js 호환은 명시돼 있지 않다.** SSR 가능 여부도 문서에 없다. 현행 Next.js 앱 라우터 자산을 그대로 재사용할 수 있다고 가정하면 안 된다.

---

## 7단계(커뮤니티)를 여는 조건

아래를 **전부** 만족할 때만 재검토한다. 하나라도 빠지면 열지 않는다.

- [ ] 저장 유저의 재방문율이 유의미하다 — 커뮤니티 없이도 돌아오는 사람이 있다는 증거
- [ ] 모더레이션에 쓸 시간이나 사람이 확보됐다
- [ ] 유저가 **자발적으로** 쓰려는 신호가 관측됐다 (예: 신고 폼 자유 텍스트에 긴 글이 반복 유입)
- [ ] 앞 단계에서 수익이 이미 발생 중이다

셋째가 가장 중요하다. **수요가 관측된 뒤에 여는 것**이지, 열어놓고 오기를 기다리는 것이 아니다. 후자는 빈 게시판만 남는다.

---

## ⚠️ 선행 과제 — 없어진 근거 문서

`supabase/migrations/0001_init.sql` 이 근거로 인용하는 두 문서가 **리포에 없다.** 인용은 11군데다.

| 인용 위치 | 인용된 문서 | 내용 |
|---|---|---|
| `0001_init.sql:3` | `CONCEPT.md 6장` / `LEGAL.md` | 스키마 설계 근거 · 법적 제약 전반 |
| `0001_init.sql:25` | `LEGAL.md 1.3` | — |
| `0001_init.sql:31` | `LEGAL.md 4.7` | 정보통신망법 제44조의3 — 채널 단위 즉시 비공개 |
| `0001_init.sql:35` | `LEGAL.md 4.5` | YouTube API §III.E.2 — 조회수·구독자수 교차집계 금지 |
| `0001_init.sql:69` | `LEGAL.md 4.5` | 월 1회 갱신 배치는 정책 요건 |
| `0001_init.sql:109` | `LEGAL.md 4장` | 구글 약관 — Places 콘텐츠를 비-구글 지도에 표시 금지 |
| `0001_init.sql:116` | `LEGAL.md 4.5` | AdSense 독립적 가치 요건 |
| `0001_init.sql:121` | `LEGAL.md 4.6` | 동명이점 오확정 = 유일한 구조적 리스크 |
| `0001_init.sql:147` | `CONCEPT.md 7.1` | 타임스탬프 70% 이상이 조각 완성 기준 |
| `0001_init.sql:172` | `LEGAL.md 4.7` | 임시조치 30일 이내 결론 |
| `0001_init.sql:192` | `CONCEPT.md 4.7` | 검색 실패 로그의 활용 |

리포의 마크다운은 `PRODUCT.md` 와 이 문서뿐이다.

**2단계(처리방침 개정)와 4단계(AdSense)는 `LEGAL.md` 없이 제대로 진행할 수 없다.** 인증·광고·UGC 어느 쪽이든 착수 전에 복원하거나 다시 쓴다.

---

## 한 줄 요약

유저를 모으는 것은 커뮤니티가 아니라 **저장 기능 + 쇼츠 유입**이다.
유저 참여는 **글이 아니라 신고·제보 폼**으로 받는다.
커뮤니티는 **스키마만 열어두고 기능은 닫아둔다.**
