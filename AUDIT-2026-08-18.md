# 품질 감사 보고서 — 2026-08-18

전 영역(성능·렌더링·SEO·보안·UX·접근성·인프라)을 감사하고, 발견 즉시 수정했다.
최종 규모: **46개 파일 변경/신설, +475/-179 라인 + 신규 파일 5개**
(requireAdmin 가드, LocaleSwitch, OG 이미지 3종). 모든 수정은
`npm run build` · `tsc --noEmit` · `npm run lint`(에러 0) 를 통과한 상태다.

## 요약

| 영역 | 발견 | 수정 | 잔여(권장) |
|---|---|---|---|
| 인프라 | 2 | 2 | 1 |
| SEO | 2 | 1 | 1 |
| 보안 | 6 | 4 | 2 |
| 성능·렌더링 | 3 | 2 | 1 |
| UX·접근성 | 12 | 10 | 2 |
| 코드 품질 | 10 | 10 | 0 |

가장 중요한 발견 셋:

1. **vercel.json 크론 삭제 사고** — YouTube 정책 준수 배치가 스케줄에서 빠져 있었다 (복원 완료).
2. **sitemap.ts 1000행 절단** — 장소·연결이 1000행을 넘는 순간 사이트맵이 조용히 잘리는 버그 (수정 완료).
3. **홈 모바일에서 검색 진입로 상실** — 히어로를 지나 스크롤하면 검색으로 돌아올 길이 없었다 (수정 완료).

---

## 1. 인프라

### 1-1. [수정] vercel.json 크론 복원 — YouTube 정책 위반 상태였다

커밋 `bdb90c0` 이 무관한 작업 중에 `vercel.json` 의 `crons` 블록을 통째로 지웠다.
`/api/cron/refresh-youtube-meta` 는 YouTube API 정책(§III.E.4.d, Non-Authorized Data
30일 보관 한도)을 지키는 배치라서, 스케줄이 빠진 순간부터 메타데이터가 갱신 없이
늙고 있었다. 매일 03:00 스케줄로 복원했다.

> **직접 확인 필요**: Vercel 프로젝트 환경변수에 `CRON_SECRET` 이 실제로 설정돼
> 있는지, 배포 후 Vercel 대시보드 → Crons 에 항목이 잡히는지 확인할 것.

### 1-2. [수정] 보안 응답 헤더 추가 (next.config.ts)

비어 있던 설정에 표준 헤더를 넣었다: `X-Content-Type-Options: nosniff`,
`Referrer-Policy: strict-origin-when-cross-origin`, `X-Frame-Options: DENY`,
`Permissions-Policy: camera=(), microphone=(), geolocation=(self)` (지도 "현재 위치"가
geolocation 을 쓰므로 self 유지).

### 1-3. [잔여] Supabase 규모

인덱스는 현 쿼리 패턴과 일치하고(saved/user 계열 복합 인덱스 포함), RLS 는 13개
테이블 전부 걸려 있다. 현 데이터 규모(~수천 행)에서는 추가 튜닝이 무의미하다.
장소가 수만 단위로 커지면 `video_places` 전 테이블 스캔 계열(fetchAll)을 RPC 집계로
바꿀 것.

## 2. SEO

### 2-1. [수정] sitemap.ts 가 1000행에서 잘리고 있었다 (P0)

최근 커밋들(`d49e695`, `2a8e575`, `385d191`)이 홈·그래프·검색 색인의 1000행 절단을
고쳤지만 **사이트맵은 빠져 있었다**. `creators`·`cities`·`places`·`videos`·`video_places`
전부 bare select 라 1000행 초과분이 조용히 사라진다 — 그 너머의 도시·조각 페이지는
검색엔진에 광고되지 않는다. 특히 `video_places` 는 `.in(place_id 수백 개)` 패턴이라
URL 길이 제한으로 통째로 실패할 수 있는 형태였다(chunked-in.ts 주석의 재현 사례와
동일). 다섯 쿼리 모두 `fetchAll` 로 교체하고 `.in()` 을 제거했다(후속 루프가 어차피
JS 에서 확정 장소만 거른다).

### 2-2. [확인] 메타·구조화 데이터·로케일은 건강하다

- hreflang: 사이트맵 `alternates.languages` + 페이지 `generateMetadata` 짝 일치.
- JSON-LD: WebSite/Organization(루트), ItemList(홈), Place 목록(도시) 존재. XSS 이스케이프 처리됨.
- robots: /admin·/api·/hero-concepts·/login 차단, noindex 이중 방어.
- 렌더는 전부 동적이지만 **데이터 왕복은 `cachePublic`(1h TTL + 태그 무효화)** 로
  캐시된다 — cache.ts 의 설계 주석대로 동작 확인.

### 2-3. [잔여·판단 필요] 영상 페이지 noindex

`/c/[creator]/v/[videoId]` 는 의도적 noindex 다(조각 페이지와 상호명 중복 잠식 방지 —
페이지 주석에 근거 명시). 대안은 noindex 대신 **조각 페이지로 canonical** 을 걸어
링크 신호를 조각에 몰아주는 것. 지금 설계도 방어 가능하므로 강제 수정하지 않았다.
"곽튜브 도쿄 맛집" 류 롱테일을 더 먹고 싶어지면 canonical 전환을 실험할 것.

## 3. 보안 (전문 감사 에이전트 + 검증)

**P0 없음.** 시크릿 노출·service-role 유출·RLS 우회·비인증 어드민 접근 모두 없음이
확인됐다. 수정한 것:

### 3-1. [수정] 어드민 서버 액션 자체 인증 가드 (P1)

6개 액션 파일 전부가 미들웨어(proxy.ts) 쿠키 검사에만 기대고 있었다. 서버 액션은
별도 엔드포인트로 호출 가능하므로, 리팩터링으로 경로가 어긋나는 순간
service-role 권한 액션이 무방비가 되는 구조였다. `requireAdmin()` 가드를 만들어
모든 액션 첫 줄에서 쿠키 토큰을 직접 검증하게 했다 (defense in depth).

### 3-2. [수정] resolve-google-place SSRF (P1)

`isGoogleShareLink` 가 부분 문자열 매칭이라 `https://attacker.com/?x=maps.app.goo.gl`
도 통과해 서버가 임의 URL 을 fetch 했다. `new URL()` 파싱 후 호스트명 완전 일치
(`maps.app.goo.gl`, 또는 `goo.gl` + `/maps` 경로)로 잠갔다.

### 3-3. [수정] 어드민 쿠키 secure 플래그 / auth 콜백 `</script>` 이스케이프 (P2)

- 로그인 쿠키의 `secure` 조건을 명시적으로 정리.
- auth 콜백 인라인 스크립트의 `JSON.stringify` 출력에 `<` 이스케이프 추가
  (json-ld.tsx 와 같은 패턴).

### 3-4. [잔여] 낮은 위험 2건

- 어드민 로그인 rate limit 이 프로세스 메모리라 인스턴스별로 초기화된다.
  `ADMIN_SECRET` 이 256bit 랜덤이라 실질 위험은 낮음 — 강화하려면 Upstash/KV.
- `/api/search-miss` 가 공개 엔드포인트인데 service-role 클라이언트를 쥔다.
  현재 핸들러는 잠긴 RPC 하나만 부르므로 안전하나, 수정 시 주의가 필요한 경계다.

## 4. 성능·렌더링

### 4-1. [수정] /map 목록 1000행 DOM (P1)

필터 없는 `/map` 은 확정 장소 전부를 `<ul>` 로 한 번에 그렸다 — 행마다 썸네일
프레임이 있어 DOM 만으로 모바일이 눌린다. **48행 점진 렌더링**을 넣었다:
필터가 바뀌면 캡 리셋, 지도 핀으로 캡 밖 행을 고르면 그 행까지 자동 확장,
꼬리는 IntersectionObserver + "더 보기" 버튼 겸용. 지도 핀은 여전히 전체를 받는다.

### 4-2. [수정] react-hooks 린트 에러 8건 — 전부 실제 렌더 위생 문제

- `Thumb`·`PlaceSheet`·`HomeCanvas`: prop 변경 시 상태 리셋을 이펙트에서 렌더 중
  조정으로 옮김 — 이펙트로 미루면 한 프레임 동안 이전 상태로 그려지는 깜빡임이
  있었다(예: 새 장소가 이전 스냅 높이로 뜨는 것).
- `MapView`·`PlaceSheet`·`HomeCanvas`: 렌더 중 ref 대입 3곳을 이펙트 동기화로 —
  읽는 곳이 전부 이벤트 콜백임을 확인하고 옮겼다.
- `ListPicker`: SSR 포탈 mount 플래그를 `useSyncExternalStore` 하이드레이션 패턴으로.
- `SavedContext`: `toggleSavedCb` 의 의존성 누락 — 롤백 시 **낡은 그룹 멤버십을
  복원하는 실제 버그**였다. 의존성 추가로 수정.

### 4-3. [확인·잔여] 구조 판단

- 폰트(사용 5단만 셀프호스팅, preload 억제), 썸네일(유튜브 CDN 직결 — next/image
  프록시 회피는 올바른 판단), 데이터 캐시(`cachePublic`) 모두 이미 잘 잡혀 있다.
- `/map` 은 장소 전체를 RSC payload 로 내린다 — 현 규모(수천)에서는 즉답형 필터의
  대가로 합리적. 수만 단위가 되면 뷰포트 기반 로딩으로 전환할 것.
- 채널당 영상 1000편 초과 시 허브 목록이 잘린다(`loadCreatorVideos` 에 range 없음).
  현 데이터에선 미발생 — 대형 채널 추가 시 fetchAll 로 바꿀 것.

## 5. UX·접근성 (전문 감사 에이전트 + 검증)

### 수정 완료

| # | 문제 | 수정 |
|---|---|---|
| 1 | 필터 패널 검색 입력이 13px → iOS 포커스 자동 확대 (PRODUCT.md 위반) | 16px 로 |
| 2 | 필터 패널 `bg-white` 하드코딩 | `bg-(--sheet)` 토큰으로 |
| 3 | 홈 히어로에 가치 제안 문장 없음 (`blurb` 빈 문자열 + 미렌더) | ko/en 문구 채우고 h1 아래 렌더 |
| 4 | **홈 모바일: 스크롤하면 검색 진입로 소멸** (헤더 돋보기가 홈에서 무조건 숨김) | 히어로 검색 알약이 뷰포트에 보일 때만 숨기고, 지나가면 헤더가 이어받음 (IO) |
| 5 | 빈 결과 화면이 막다른 길 (EmptyState 자기 주석 위반) | "필터 지우기" 액션을 빈 화면 본문에 추가 |
| 6 | **KO/EN 전환 UI 부재** — Accept-Language 판정이 틀린 사용자가 못 되돌림 | 푸터에 `LocaleSwitch` 신설 (넘어갈 언어를 그 언어로 표기, 쿼리 보존) |
| 7 | 도시 페이지 데스크톱에 위로 갈 내비 없음 (브레드크럼 `lg:hidden`) | 브레드크럼은 전 해상도, 제목·통계만 모바일 전용 |
| 8 | 필터 드롭다운 포커스 관리 부재 (WCAG 2.1 AA) | aria-modal + 포커스 트랩 + 닫힘 시 트리거 복귀. 모바일은 키보드가 지도를 덮지 않게 자동 포커스 억제 |
| 9 | 검색 빈 결과가 무안내 종착지 | 힌트(짧은 단어·한/영 전환) + "지도에서 직접 찾아보기" 출구 추가 (ko/en) |
| 10 | 외부 링크(유튜브·지도) 새 탭 전환 무예고 (WCAG G200) | OutboundA 에 sr-only 안내 (ko/en) |
| 11 | reduced-motion 에서 스켈레톤 시머가 정지 줄무늬로 잔존 | `background-image: none` 추가 |
| 12 | 지도 "전체 핀 보기" 아이콘 버튼 툴팁 없음 | `title` 추가 |

### 잔여(선택)

- 히어로 배경 이미지의 `fetchPriority="high"` — 장식 이미지가 LCP 대역폭을 다툰다는
  지적이 있었으나, 실측 없이 바꾸면 오히려 LCP(가장 큰 요소가 그 이미지일 가능성)를
  해칠 수 있어 보류. Vercel Speed Insights 로 실측 후 판단할 것.
- CategoryGrid 타일 간격(gap-x 4px)이 촘촘하다는 지적 — 디자이너가 줄바꿈 방지와
  함께 튜닝한 값이라 유지. 오탭 제보가 있으면 재고.

## 6. 코드 품질

- **phosphor 아이콘 deprecation 14개 파일 정리** — v2.1.10 이 bare export 를
  deprecated 로 바꿈. `MagnifyingGlassIcon as MagnifyingGlass` 별칭 패턴으로 import
  줄만 바꿔 사용처 diff 0 으로 처리.
- `tmp/` 스크래치 스크립트를 eslint ignore 에 추가 (이미 gitignore 대상).
- 최종 상태: **lint 에러 0** (경고 2: HomeSheet 히어로 `<img>` 는 의도적 —
  scripts/ 스크립트 미사용 변수 1).

## 7. 검증

```
npm run build     ✓ (24/24 페이지, 에러 0)
npx tsc --noEmit  ✓
npm run lint      ✓ 에러 0
```

## 7-1. 2차 수정 (전문 감사 에이전트 보고 반영)

성능·SEO 전문 감사가 추가로 도착해 다음을 더 수정했다:

### 성능

- **[수정] 파생 데이터 캐시 누락 (P0)** — `loadCityIndex`·`loadHomeMap`·`loadCityDetail` 이
  행 데이터(loadGraphRows)만 캐시하고 그래프 순회·Map 구성은 요청마다 다시 돌았다.
  `/map` 한 번에 같은 그래프를 3번 조립(수십~150ms TTFB 낭비). 셋 다 `cachePublic` 로
  감쌌다 — locale·citySlug 인자는 캐시 키에 자동 포함되므로 로케일 오염이 없다.
- **[수정] 드래그 핸들러의 matchMedia 남발 (P1)** — 시트 드래그·스크롤 핸들러가
  이벤트마다(초당 수십 회) `MediaQueryList` 를 새로 만들어 GC 를 조였다.
  change 리스너로 갱신되는 ref 하나로 교체.
- **[수정] 핀 선택 시 전체 마커 DOM 재구성 (P1)** — activeId 가 바뀔 때마다 수백
  마커 전부 `replaceChildren` 하던 것을 이전 활성·새 활성 2개만 갱신하게.
- **[수정] markerclusterer 동적 import (P2)** — 클러스터를 쓰지 않는 화면까지
  supercluster(~12KB gzip)를 물던 정적 import 를 지도 SDK 로드와 같은 Promise.all
  안의 동적 import 로.
- **[수정] 번들 설정 (P2)** — `serverExternalPackages: [@supabase/*]`,
  `optimizePackageImports: [@phosphor-icons/react]` 추가.
- **[수정] 미사용 의존성 (P2)** — `@anthropic-ai/sdk` 제거(코드 어디서도 미사용),
  `openai` 는 로컬 스크립트(translate-en.mjs) 전용이라 devDependencies 로 이동.

### SEO

- **[수정] OG 이미지 3종 신설 (P0)** — `/channels` · `/type/[type]` · `/c/[creator]`(허브)에
  opengraph-image.tsx 가 없어 공유 카드가 루트 기본값으로 떨어졌다. 기존
  조각 페이지 카드와 같은 문법(타이포 중심, 산호 밑줄 악센트, 썸네일 미변형)으로
  제작 — 셋 다 빌드에 라우트로 등록 확인. 잘못된 슬러그·종류는 크래시 대신
  일반 카드로 폴백한다.

### 감사 보고 중 채택하지 않은 것 (근거)

- **LocalBusiness/VideoObject 스키마 추가** — json-ld.tsx 상단에 법적 근거가 명시된
  의도적 배제다(장소 평가 금지, YouTube §III.E.2). 현행 Place+GeoCoordinates 유지.
- **BreadcrumbList 누락 주장** — 실제로는 허브·조각·종류 페이지 전부 이미 있다 (오탐).
- **정적 페이지 4곳 메타데이터 누락 주장** — 4곳 모두 `generateMetadata` 보유 (오탐).
- **supabase remotePatterns** — next/image 로 원격 이미지를 받는 곳이 없다 (오탐).
- **파셋 카운트 단일 패스 통합(P1)** — 필터 변경당 ~2-5ms 수준이라 리팩터링 위험 대비
  이득이 작아 보류.
- **핀 시그니처 문자열 → 참조 비교** — 호출부가 매 렌더 배열을 재생성하는 구조라
  참조 비교로 바꾸면 시그니처의 존재 이유(내용 같으면 뷰포트 유지)가 깨진다. 유지.

## 8. 다음 액션 제안 (유입 확대 관점, 우선순위순)

1. **배포 후 확인**: Vercel Crons 등록 + `CRON_SECRET` env, 보안 헤더 응답 확인.
2. **Search Console**: 사이트맵 재제출 (1000행 수정으로 URL 수가 늘었을 수 있다),
   "리디렉션이 있는 페이지" 잔량 추이 확인.
3. **Speed Insights/Analytics 활성 확인**: 히어로 이미지 LCP 실측 → fetchPriority 판단.
4. **콘텐츠가 최대 레버**: 이 제품의 SEO 는 확정 장소 수가 곧 페이지 수다.
   기술 부채보다 어드민 확정 큐 처리량을 올리는 것이 유입에 직결된다.
5. 장소 수만 단위 도달 시: `/map` RSC payload 뷰포트 로딩, video_places RPC 집계 전환.
