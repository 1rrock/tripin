# Tripin — 웹 컨셉 기획서

> README.md(제품 기획)의 후속 문서.
> "무엇을 만드는가"가 아니라 **"웹으로 어떻게 만들 것인가"** 를 페이지·기능·콘텐츠 단위로 확정하기 위한 문서.
> 작성: 2026-08-03 · 상태: 초안(구현 착수 전 합의용)

---

## 0. 이 문서가 정하는 것

| 섹션 | 내용 |
|------|------|
| 1~3 | 제품 컨셉을 화면 언어로 번역 (핵심 경험, 정보구조, URL) |
| 4 | **페이지별 상세 기능 명세** (레이아웃 / 데이터 / 상호작용 / SEO / 완성 기준) |
| 5 | 공통 컴포넌트 사양 |
| 6 | 데이터 모델 확정안 (README 8장 보강) |
| 7 | **콘텐츠 제작 계획** (무엇을, 어떤 순서로, 어떤 품질로, 얼마나 빨리) |
| 8 | 콘텐츠 파이프라인 & 어드민 |
| 9 | SEO / AdSense 실행 계획 |
| 10 | 단계별 로드맵과 지표 |

---

## 1. 웹 컨셉 한 줄

> **"채널을 고른다 → 지도가 채워진다 → 핀을 누르면 그 영상으로 간다."**

이 3스텝이 3번의 클릭 안에, **첫 화면 이탈 없이** 일어나야 한다.
그 외 모든 기능(검색, 필터, 저장, 계정)은 이 3스텝을 방해하지 않는 선에서만 존재한다.

### 1.1 컨셉을 지탱하는 4개의 UI 원칙

| 원칙 | 화면에서의 의미 |
|------|-----------------|
| **P1. 지도는 결과지 입구가 아니다** | 홈에 세계지도를 깔지 않는다. 홈은 **크리에이터 그리드**. 빈 지도는 "아무것도 없는 서비스"로 읽힌다 |
| **P2. 밀도를 먼저 보여준다** | 크리에이터 카드에 `핀 42 · 도시 6 · 영상 18` 을 항상 노출. 이게 곧 신뢰이자 제품 |
| **P3. 미확인은 숨기지 않고 격리한다** | `confirmed`만 지도에 강조 핀. `candidate`는 리스트 하단 회색 섹션 "위치 확인 중". `none`은 비공개 |
| **P4. 모든 핀은 출처로 끝난다** | 어떤 경로로 들어와도 마지막 클릭 대상은 **YouTube 영상 링크 + 지도 앱 열기** 두 개 |

### 1.2 톤 & 비주얼 방향

- **지도 UI 톤**: 밝은 중성 베이스맵(회색·연한 지형) + 핀만 채도 높게. 크리에이터별 액센트 컬러 1개를 핀에 적용.
- **텍스트 톤**: 정보 나열형. 감탄사·여행 카피 금지("인생 맛집!" X). `돈카츠. 도쿄 고토구. 영상 12:40 언급.` 수준.
- **폰트/밀도**: 리스트는 밀도 높게(모바일 1행 72~88px), 지도는 여백 있게.
- **다크모드**: 지도 타일 2종 필요 → **Phase 2로 미룸**. 초기엔 라이트 고정.
- **모바일 우선**: 트래픽 대부분 모바일 검색 유입. 모든 페이지를 375px 기준으로 먼저 설계.

---

## 2. 정보 구조 (IA)

```
Home  (크리에이터 그리드)
├── /c/[creator]                    크리에이터 허브 — 세계 지도 + 도시 목록
│   └── /c/[creator]/[city]         ★ 핵심 SEO 페이지 — 도시 지도 + 핀 리스트
│       └── /c/[creator]/[city]/[place]   장소 상세
├── /city/[city]                    도시 교차 페이지 — "도쿄에 간 유튜버들"
├── /place/[place]                  장소 정규 페이지 (여러 채널이 언급한 경우)
├── /search                         통합 검색
└── /about, /policy, /takedown, /privacy   고지·정책

/admin/*                            비공개 운영 도구
```

### 2.1 두 개의 축, 두 개의 진입

| 축 | 진입 경로 | 사용자 의도 |
|----|-----------|-------------|
| **채널 축** (주) | 홈 → 크리에이터 → 도시 | "곽튜브가 간 데 보고 싶다" |
| **도시 축** (부) | 검색 유입 → `/city/tokyo` → 채널 선택 | "도쿄 가는데 유튜버 맛집" |

두 축은 `/c/[creator]/[city]` 에서 **만난다**. 이 페이지가 트래픽·SEO·전환의 단일 허브다.
→ **모든 개발·콘텐츠 우선순위는 이 페이지를 기준으로 잡는다.**

### 2.2 URL 규칙

| 패턴 | 예시 | 렌더링 |
|------|------|--------|
| `/` | | SSG (ISR 1h) |
| `/c/[creator]` | `/c/kwaktube` | SSG |
| `/c/[creator]/[city]` | `/c/kwaktube/tokyo` | SSG ★ |
| `/c/[creator]/[city]/[place]` | `/c/kwaktube/tokyo/marushichi-tonkatsu` | SSG |
| `/city/[city]` | `/city/tokyo` | SSG |
| `/place/[place]` | `/place/marushichi-tonkatsu` | SSG |
| `/search?q=` | | CSR (noindex) |

- slug는 **영문 소문자 + 하이픈** 고정. 한글 URL 금지(공유 시 깨짐).
- 도시 slug는 `tokyo`, `osaka`, `da-nang` 처럼 **국가 코드 없이**. 충돌 시 `porto-pt` 식 접미사.
- 장소 slug는 `[영문 상호]-[타입]` → `marushichi-tonkatsu`. 중복 시 `-2`.
- **slug는 절대 변경하지 않는다.** 변경 시 301 리다이렉트 테이블 필수.

---

## 3. 화면 골격 (전 페이지 공통)

```
┌─────────────────────────────────────┐
│ Header: 로고 | 크리에이터 | 도시 | 🔍 │  ← 56px, 스크롤 시 고정
├─────────────────────────────────────┤
│ Breadcrumb (SEO + 방향감)             │  ← 홈 제외 전 페이지
├─────────────────────────────────────┤
│                                     │
│           Page Content              │
│                                     │
├─────────────────────────────────────┤
│ Disclaimer 바 (비공식 · 제휴 아님)     │  ← 전 페이지 고정 문구
│ Footer: 정책 · 삭제요청 · 문의        │
└─────────────────────────────────────┘
```

**고정 고지 문구 (전 페이지 푸터 상단)**
> Tripin은 공개된 영상 정보를 정리한 **비공식** 디렉터리입니다. 각 크리에이터·채널과 제휴 관계가 없으며, 모든 장소 정보에는 출처 영상이 표기됩니다. 삭제·수정 요청은 [여기](/takedown)로 접수해 주세요.

---

## 4. 페이지별 상세 기능

---

### 4.1 `/` — 홈 (크리에이터 그리드)

**목적**: 3초 안에 "이건 유튜버별 지도구나" 를 이해시키고 채널 카드 클릭까지 유도.

#### 레이아웃 (모바일 → 데스크톱)

```
[히어로]
  H1: 여행 유튜버가 간 곳, 지도로.
  Sub: 채널을 고르면 그 사람이 다녀간 맛집·명소가 지도에 뜹니다.
  [🔍 채널·도시·장소 검색]           ← 히어로 내 검색바

[요약 스탯 바]
  크리에이터 5 · 도시 9 · 확정 장소 214              ← 우리 큐레이션 산출물만 집계
  ⚠️ 조회수·구독자수·좋아요 표시 금지 (YouTube API §III.E.2 교차집계 제한)

[필터 칩]  전체 | 🇰🇷한국어 | 🇺🇸영어 | 먹방 | 배낭 | 럭셔리

[크리에이터 카드 그리드]  모바일 1열 / 태블릿 2열 / 데스크 3열
  ┌──────────────────────────────┐
  │ [이니셜뱃지] 곽튜브            │   ← ⚠️ 프로필 사진 사용 금지 (LEGAL.md 1.3)
  │           Kwaktube            │
  │  📍 42 핀 · 🏙 6 도시 · 🎬 18 영상 │
  │  최근: 도쿄 · 다낭 · 이스탄불    │   ← 도시칩, 각각 딥링크
  │  [지도 보기 →]                 │
  └──────────────────────────────┘

[도시로 찾기]  ← 보조 진입
  도쿄(3채널) 오사카(2) 다낭(2) 방콕(1) ...

[이 서비스는?]  3줄 설명 + /about 링크
```

#### 기능 명세

| 기능 | 상세 |
|------|------|
| 크리에이터 정렬 | 기본 `확정 핀 수 DESC`. 빈약한 채널이 위에 오면 안 됨 |
| 필터 칩 | 언어(ko/en) + 태그(먹방/배낭/럭셔리/커플). 클라이언트 필터, URL 쿼리 반영 `?lang=ko` |
| 카드 클릭 | 카드 전체 → `/c/[creator]`. 도시 칩 → `/c/[creator]/[city]` 직행 |
| 검색바 | 클릭 시 `/search` 이동 (홈에서 인라인 결과 X — 복잡도 대비 효익 낮음) |
| 준비중 채널 | **표시하지 않는다.** 핀 0개 채널은 그리드에서 제외 (P2 원칙) |
| 스탯 바 | 빌드 타임 집계값. 숫자가 작아도 노출 (정직 > 과장) |

#### SEO
- `title`: `여행 유튜버가 간 곳 지도 | Tripin`
- `description`: 채널명 3~5개를 실제로 나열 → 브랜드 검색 흡수
- 구조화 데이터: `WebSite` + `SearchAction`
- 크리에이터 카드는 **SSR HTML로 렌더** (JS 없이 크롤 가능해야 함)

#### 완성 기준 (DoD)
- [ ] JS 비활성 상태에서 크리에이터 목록·링크가 HTML에 존재
- [ ] LCP < 2.0s (모바일 4G), 히어로 이미지 없음 또는 최적화
- [ ] 카드 클릭 → 크리에이터 페이지 전환 CLS 0

---

### 4.2 `/c/[creator]` — 크리에이터 허브

**목적**: "이 사람의 전 세계 발자국"을 한 화면에 보여주고, **도시 페이지로 내려보내기**.

#### 레이아웃

```
[Breadcrumb]  홈 › 곽튜브

[프로필 헤더]
  [아바타] 곽튜브 / Kwaktube
  🇰🇷 한국어 · 여행/먹방
  📍 42 확정 · 🏙 6 도시 · 🎬 18 영상 · 최근 갱신 2026-07-28
  [YouTube 채널 열기 ↗]        ← 원본으로 보내는 링크, 숨기지 않음
  ⚠️ 비공식 · 제휴 아님

[세계 지도]   높이 380px(모바일) / 520px(데스크)
  - 도시 단위 클러스터 마커 (핀 개수 뱃지)
  - 클러스터 클릭 → 해당 도시 페이지로 이동 (지도 줌인 X)
  - 지도 위 오버레이 없음. 조작 최소화

[도시 카드 리스트]  ← 실질적 네비게이션
  ┌────────────────────────────────┐
  │ 🇯🇵 도쿄  Tokyo, Japan          │
  │ 📍 12곳 · 🍜 8 · 🏛 3 · ☕ 1     │
  │ 대표: 돈카츠 마루시치, 시부야 스카이…│
  │ [지도 보기 →]                    │
  └────────────────────────────────┘
  (핀 수 DESC 정렬)

[영상 타임라인]  접힘 기본
  2026-06-12  도쿄 3박4일 먹방편   → 5곳
  2026-05-30  오사카 당일치기      → 3곳

[다른 크리에이터도 보기]  ← 이탈 방지 크로스링크
```

#### 기능 명세

| 기능 | 상세 |
|------|------|
| 지도 마커 | **도시 클러스터만.** 개별 장소 핀은 이 레벨에서 렌더하지 않음 (성능 + 인지부하) |
| 클러스터 클릭 | `/c/[creator]/[city]` 이동. 지도 내 확대는 하지 않음 → 도시 페이지가 SEO 자산이므로 반드시 URL 이동 |
| 지도 미로드 대비 | 지도 로딩 실패/JS off 시 도시 카드 리스트만으로 완전 사용 가능 |
| 영상 타임라인 | 영상 → 장소 역방향 탐색. 접힘 기본(모바일 스크롤 보호) |
| 빈 도시 | 확정 핀 0인 도시는 노출 안 함 |

#### SEO
- `title`: `곽튜브가 간 곳 지도 — 도쿄·다낭·이스탄불 | Tripin`
- 구조화 데이터: `ProfilePage` + `BreadcrumbList`
- 내부 링크: 도시 카드가 곧 내부 링크 허브 → 도시 페이지 색인 촉진

---

### 4.3 `/c/[creator]/[city]` — ★ 채널×도시 (핵심 페이지)

**목적**: 검색 유입 → 지도 확인 → 영상/지도앱으로 아웃링크. **이 페이지가 제품이다.**

#### 레이아웃 (모바일 = 지도/리스트 토글, 데스크 = 좌우 분할)

```
모바일
┌────────────────────────────┐
│ Breadcrumb 홈›곽튜브›도쿄     │
│ H1 곽튜브 도쿄 간 곳 12곳     │
│ 요약 2줄 (직접 작성)          │
│ [필터: 전체|🍜맛집|🏛명소|☕카페]│
├────────────────────────────┤
│         지도 (40vh)         │  ← 상단 고정
│   핀 = 번호 마커 ①②③        │
├────────────────────────────┤
│ [리스트]  ← 스크롤            │
│ ① 돈카츠 마루시치   🍜        │
│    고토구 · 웨이팅 있음        │
│    "워뇨타이 추천 돈카츠."     │
│    🎬 도쿄 먹방편 12:40       │
│    [지도 열기] [영상 보기]     │
│ ② ...                       │
│ ─ 위치 확인 중 (2) ───────    │  ← candidate 격리 섹션
│ · 이름만 확인된 라멘집         │
└────────────────────────────┘

데스크톱: 좌 리스트(420px) / 우 지도(sticky, 100vh-header)
```

#### 기능 명세 (가장 중요)

| 기능 | 상세 |
|------|------|
| **핀 ↔ 리스트 연동** | 리스트 항목 호버/탭 → 해당 핀 하이라이트+팬. 핀 클릭 → 리스트 해당 항목 스크롤+하이라이트. **양방향 필수** |
| **번호 마커** | 핀에 ①②③ 번호. 리스트 번호와 일치 → "지도 보고 리스트 찾기" 인지비용 제거 |
| 마커 색 | 타입별 아이콘(🍜🏛☕🏨) + 크리에이터 액센트 컬러 |
| 필터 | 타입 필터. 지도·리스트 동시 적용. URL 쿼리 반영 `?type=food` |
| 초기 뷰포트 | 확정 핀 전체 bounds + 패딩. 핀 1개면 zoom 15 고정 |
| **candidate 섹션** | 리스트 최하단, 회색 배경, 지도 핀 없음(또는 점선 회색). 헤더 문구: `아직 위치를 확정하지 못한 곳` |
| 아웃링크 2종 | `[지도 열기]` = Google Maps place_id URL / 국내는 카카오맵. `[영상 보기]` = `youtube.com/watch?v=..&t=초` **타임스탬프 포함** |
| 타임스탬프 | `VideoPlace.timestampSec` 있으면 `&t=` 부착. 없으면 영상 첫 부분 |
| 공유 | `[링크 복사]` + Web Share API. OG 이미지는 지도 썸네일(정적 생성) |
| 지도 실패 폴백 | 지도 영역 대신 `정적 지도 이미지` 또는 리스트 전체 표시 |
| 로컬 저장 | ★Phase 2 — localStorage 즐겨찾기. 계정 없음 |

#### 성능 규칙
- 지도 라이브러리는 **dynamic import + intersection 기반 지연 로드**. 리스트는 즉시 SSR.
- 핀 데이터는 페이지 HTML에 JSON 인라인(≤50개면 fetch 불필요).
- 지도 스크립트가 LCP에 포함되지 않도록 히어로/H1/요약을 지도 위에 배치.

#### SEO (이 페이지에 SEO 자원 집중)
- `title`: `곽튜브 도쿄 맛집·간 곳 지도 (12곳) | Tripin`
- `h1`: `곽튜브가 도쿄에서 간 곳`
- `description`: 상위 3개 상호명 실명 포함 → 롱테일 `곽튜브 마루시치` 흡수
- 구조화 데이터: `ItemList` (각 item = `Place`), `BreadcrumbList`
- **본문 텍스트 300자 이상 확보** — 도시 요약 2~3문장 + 각 장소 요약. AdSense 승인·색인 최소 조건
- 내부 링크: 장소 상세 / 같은 도시 다른 채널(`/city/tokyo`) / 같은 채널 다른 도시

#### 완성 기준 (DoD)
- [ ] 확정 핀 ≥ 8개 (미만이면 페이지 공개 금지 — 7장 참조)
- [ ] 모든 장소에 출처 영상 링크 존재
- [ ] JS 없이 리스트+링크 크롤 가능
- [ ] 모바일에서 지도↔리스트 전환 없이 스크롤만으로 전부 열람 가능

---

### 4.4 `/c/[creator]/[city]/[place]` — 장소 상세

**목적**: 롱테일 검색(`마루시치 돈카츠 곽튜브`) 착지점 + 아웃링크 종착.

```
[Breadcrumb] 홈›곽튜브›도쿄›돈카츠 마루시치

H1  돈카츠 마루시치   (とんかつ まるしち)
🍜 돈카츠 · 🇯🇵 도쿄 고토구
📍 주소 한 줄
[지도 앱에서 열기 ↗]  [길찾기 ↗]

[요약]  3~5문장, 직접 작성, 팩트만
  · 워뇨타이가 추천한 곳으로 소개됨
  · 평일 오픈 전 대기 발생
  · 로스카츠 2,000엔대

[미니 지도]  200px, 핀 1개, 클릭 시 지도앱

[언급 영상]   ← 복수 가능
  🎬 곽튜브 · 도쿄 3박4일 먹방  2026-06-12
     12:40 언급  [영상 보기 ↗]
  🎬 다른채널 · 도쿄 돈카츠 투어  2026-03-02
     04:12 언급  [영상 보기 ↗]

[같은 도시 근처]  이 채널의 다른 핀 3개
[이 장소를 다녀간 다른 크리에이터]  ← 있으면
```

| 기능 | 상세 |
|------|------|
| 요약 규칙 | **대사 인용 금지.** 영상에서 확인된 사실만 불릿 3~5개. 7.3 템플릿 준수. 가격·영업정보는 **"영상 촬영 시점 기준"** 표기 필수 (LEGAL.md 4.6) |
| 언급 영상 | `VideoPlace` N:M 전개. 타임스탬프 오름차순 |
| 미확정 장소 | `candidate`는 이 페이지를 **생성하지 않음** (`noindex` 대신 미생성). 도시 페이지 리스트에만 존재 |
| 지도앱 링크 | `googlePlaceId` 있으면 `https://www.google.com/maps/place/?q=place_id:{id}`. 국내는 카카오. 둘 다 없으면 좌표 URL |
| 구조화 데이터 | `Place`(name, address, geo) — ⚠️ 리뷰/평점 스키마는 **넣지 않음**(우리가 평가하는 게 아님) |

---

### 4.5 `/city/[city]` — 도시 교차 페이지

**목적**: `도쿄 유튜버 맛집` 같은 채널 무관 검색 흡수 → 채널 페이지로 분배.

```
H1  도쿄에 간 여행 유튜버들
요약: 3개 채널이 도쿄에서 27곳을 다녀왔습니다.

[채널 카드]  ← 이 도시 기준
  곽튜브 12곳 [지도 보기]
  빠니보틀 9곳 [지도 보기]
  Kara and Nate 6곳 [지도 보기]

[통합 지도]  전 채널 핀, 채널별 색상 구분 + 범례
  ← 채널 필터 토글

[많이 언급된 곳]  2개 이상 채널이 간 장소 = 신뢰 시그널
  🔥 시부야 스카이  (3채널)
  🔥 돈카츠 마루시치 (2채널)

[도시 정보 3줄]  직접 작성 (여행 정보 아님, 데이터 요약)
```

- **차별 포인트**: `여러 채널이 겹친 장소` 섹션. 이건 경쟁 서비스가 못 만드는 유일한 뷰.
- 채널 1개뿐인 도시는 이 페이지 대신 채널×도시로 **301** (중복 콘텐츠 방지).

---

### 4.6 `/place/[place]` — 장소 정규 페이지

- **2개 이상 채널**이 언급한 장소에만 생성.
- 1개 채널 장소는 `/c/[creator]/[city]/[place]` 가 canonical이고 이 URL은 생성하지 않는다.
- 내용: 장소 정보 + **언급한 모든 채널·영상 타임라인**.
- canonical 규칙을 명확히 해서 중복 색인 페널티 회피.

---

### 4.7 `/search` — 통합 검색

| 항목 | 내용 |
|------|------|
| 대상 | 크리에이터명(한/영/채널핸들), 도시명(한/영), 장소명(한/영/현지어) |
| 구현 | Phase 1: 전체 인덱스 JSON(<200KB) 클라이언트 다운로드 + Fuse.js 유사 매칭. DB 검색 불필요 |
| 결과 | 섹션 분리 — `크리에이터` / `도시` / `장소` |
| 빈 결과 | "아직 없는 채널입니다" + **요청 폼**(채널 URL 입력) → 수요 수집 = 콘텐츠 백로그 소스 |
| SEO | `noindex` |

> 검색 빈 결과의 요청 폼은 **콘텐츠 우선순위를 정하는 최고의 데이터**다. Phase 1에 반드시 포함.

---

### 4.8 정적 페이지

| 경로 | 내용 |
|------|------|
| `/about` | 무엇을·왜·어떻게 만드는지. 데이터 출처와 검수 방식 설명(신뢰) |
| `/policy` | 콘텐츠 정책 — 자막 원문 미보관, 요약 자체 작성, 출처 표기 원칙 |
| `/takedown` | **삭제·수정 요청 폼** (채널/장소 URL, 사유, 연락처).<br>⚠️ 단순 폼이 아니라 **정보통신망법 §44조의2 법정 절차**로 구현: 접수 → 지체 없이 조치 → **신청인·게재자 양쪽 통지** → 임시조치 시 **30일 내** 결론. **약관에 절차 명시가 법정 의무**(§44조의2⑤). 크리에이터 요청 시 채널 단위 즉시 비공개(§44조의3). LEGAL.md 4.7 |
| `/privacy` | 개인정보·쿠키·AdSense 광고 쿠키 고지 (AdSense 승인 필수 요건) |

---

### 4.9 `/admin` — 운영 도구 (비공개)

콘텐츠 원가가 곧 제품 원가이므로, **어드민 품질이 곧 서비스 확장 속도**다.

| 화면 | 기능 |
|------|------|
| `/admin/ingest` | YouTube URL 붙여넣기 → 메타 수집 → 자막 기반 장소 후보 추출 → **후보 카드 리스트**.<br>⚠️ 영상 목록은 **`playlistItems.list`(1 unit)** 사용 — `search.list`는 100 units라 일 ~100회 한계 (LEGAL.md 4.5) |
| `/admin/confirm` | 후보별: 장소 검색 결과 3~5개 제시 → **확정 / 수정 / 미확인 / 버림** 4버튼. 키보드 단축키(1/2/3/Esc).<br>⚠️ **체인·동명이점은 지점까지 특정되지 않으면 '확정' 버튼 비활성** — 오확정 방지(LEGAL.md 4.6). 확정 시 `sourceNote`에 근거(간판·지역 언급·타임스탬프) 입력 강제 |
| `/admin/place/[id]` | 요약문 작성 에디터. 템플릿 프리셋 + 글자수 카운터 + 금지어 검사(대사 복붙 감지) |
| `/admin/publish` | 채널×도시 단위 **공개 체크리스트** — 핀 8개↑ / 전 항목 출처 / **전 장소 요약 작성 완료** → 통과해야 공개 버튼 활성. 요약 미작성은 AdSense 독립적 가치 요건(YouTube API §III.G.1.d)에도 걸리므로 **법적 요건**이다 |
| `/admin/queue` | 삭제 요청 · 검색 실패 로그(수요) 확인 |

**설계 원칙**: 확정 1건당 **10초 이내**. 이 숫자가 전체 사업성을 결정한다.
- 자막 원문은 화면에 띄우되 **DB 저장 안 함**(세션 메모리/임시 파일, 처리 후 폐기).

> 📄 **화면별 상세 기획은 `docs/ADMIN.md`** — 레이아웃, 상태 전이, 키맵, 인증(비밀번호+쿠키),
> 확정 잠금 규칙, 요약 검사, 공개 체크리스트, 삭제요청 법정 절차, 구현 순서.

---

## 5. 공통 컴포넌트 사양

| 컴포넌트 | 사양 |
|----------|------|
| `<MapView>` | props: `pins[]`, `mode: 'cluster'\|'numbered'`, `activeId`, `onPinClick`. 지연 로드. 실패 시 정적 이미지 폴백 |
| `<PlaceCard>` | 번호 · 아이콘 · 이름(원어 병기) · 지역 · 요약 1줄 · 영상 뱃지 · 액션 2버튼 |
| `<CreatorCard>` | **이니셜 뱃지**(액센트 컬러) · 이름(한/영) · 스탯 3종 · 도시 칩 · CTA. ⚠️ 프로필 사진·채널 로고·브랜드 서체 사용 금지 — 부정경쟁방지법 제2조 제1호 타목(초상) 리스크. LEGAL.md 1.3 |
| `<CityCard>` | 국기 · 도시명 · 핀 수 · 타입 분포 · 대표 장소 2개 |
| `<SourceLink>` | 영상 제목 + 타임스탬프 + 외부링크 아이콘. **모든 장소 노출부에서 재사용** |
| `<StatusBadge>` | `confirmed`(없음) / `candidate`(회색 "위치 확인 중") |
| `<Disclaimer>` | 전역 고지 문구 |
| `<AdSlot>` | 광고 슬롯. 위치: 리스트 6번째 뒤 / 장소 상세 하단. **지도 위·핀 근처 금지** |

---

## 6. 데이터 모델 (README 8장 확정안)

```text
Creator
  id, slug, displayName, displayNameEn,
  youtubeChannelId, youtubeHandle,
  country, languages[], tags[],        # 먹방/배낭/럭셔리/커플
  initials, accentColor, bio?,         # avatarUrl 아님 — 초상 사용 금지(LEGAL.md 1.3)
  isPublished: boolean                 # 핀 부족 시 false. 삭제요청 시 채널 단위 즉시 false
  statsCache { placeCount, cityCount, videoCount, lastUpdated }

City
  id, slug, name, nameEn, nameLocal,
  countryCode, lat, lng, defaultZoom

Video
  id, creatorId, youtubeVideoId, title, publishedAt,
  thumbnailUrl, durationSec?,
  apiFetchedAt: timestamp              # ★ 30일 내 갱신 필수 (YouTube API §III.E.4.d)
                                       #   LEGAL.md 4.5 — 무기한 보관 시 정책 위반
# 저장하지 않는 것: viewCount, likeCount, subscriberCount
#   → 교차 채널 집계 금지 조항(§III.E.2) 대상

Place
  id, slug, name, nameLocal?, nameEn?,
  cityId, countryCode,
  placeType: restaurant|cafe|attraction|hotel|bar|shop|viewpoint|other|unknown
  lat?, lng?, address?,                # ← 오픈 데이터셋(Overture/Foursquare) 출처
  googlePlaceId?, kakaoPlaceId?,       # ← 딥링크 전용. 지도 표시에 사용 금지(LEGAL.md 4장)
  mapStatus: confirmed | candidate | none
  summary: string                      # 자체 작성, 40~200자
  summaryBullets: string[]             # 3~5개
  sourceNote?, priceHint?, tips?       # "웨이팅 있음" 등 팩트만
  isPublished: boolean

VideoPlace
  videoId, placeId,
  timestampSec?,                       # 아웃링크 &t= 용
  mentionNote?,                        # 짧은 맥락 메모
  confidence: high|medium|low

CreatorCity  (파생/캐시)
  creatorId, cityId, placeCount, publishedAt, introText   # 도시 요약 2~3문장

TakedownRequest
  id, targetType, targetId, requesterEmail, reason, status, createdAt

SearchMiss  (수요 수집)
  query, count, lastSeenAt

# 절대 만들지 않는 필드
# transcriptFull, rawCaptionBlob, captionSegments, quotedDialogue
```

**설계 메모**
- `statsCache`는 빌드 시 갱신. 런타임 집계 쿼리 금지(SSG 속도).
- `accentColor`는 도시 교차 페이지 채널 구분 색으로 재사용.
- `confidence: low` 인 VideoPlace는 공개하지 않음(어드민에만 표시).

---

## 7. 콘텐츠 계획 ★

> "어떻게 컨텐츠를 넣을지"에 대한 답. **Tripin의 진짜 난이도는 코드가 아니라 여기에 있다.**

### 7.1 콘텐츠 단위 = "채널 × 도시" 한 조각

개별 장소가 아니라 **채널×도시 1조각**이 제작·공개·측정의 최소 단위다.
얇게 100개 도시를 뿌리는 대신, **한 조각을 완성해서 공개**한다. (README 원칙 7)

**한 조각의 정의 (DoD — 이걸 못 채우면 공개 안 함)**

| 항목 | 기준 |
|------|------|
| 확정 핀 | **≥ 8개** (이상적 12~20) |
| 소스 영상 | ≥ 2개 |
| 타입 다양성 | 맛집만 8개 X → 명소/카페/뷰포인트 최소 2개 포함 |
| 출처 | 100% 장소에 videoId, 70% 이상 타임스탬프 |
| 요약 | 전 장소 3~5불릿 작성 완료 (자동 생성 그대로 X) |
| 도시 인트로 | 2~3문장 직접 작성 |
| 지도앱 링크 | confirmed 전부 place_id 보유 |
| candidate 비율 | 전체의 30% 이하 |

### 7.2 제작 파이프라인 (조각 1개 기준 · 실측 목표)

```
STEP 1  영상 선정            15분   채널에서 도시 관련 본편 2~4개 고르기 (쇼츠 제외)
STEP 2  자동 추출            5분    스크립트 실행 → 장소 후보 20~40개
STEP 3  1차 필터             10분   명백한 오탐(지명·일반명사) 제거 → 15~25개
STEP 4  Places 매칭 확정     30분   후보당 10초 목표. 동명이점·지점 선택
STEP 5  요약 작성            40분   장소당 2~3분. 템플릿 기반
STEP 6  도시 인트로 + 검수    15분   오탈자·링크·타임스탬프 확인
STEP 7  공개 체크리스트       5분
─────────────────────────────────
합계                        ~2시간 / 조각
```

**주당 5~8시간 캡** (README 12장) → **주 2~3조각**, 월 8~12조각이 현실 상한.
→ 이 숫자를 로드맵과 기대치의 기준선으로 삼는다.

### 7.3 요약문 작성 규칙 (법적 안전선 = 콘텐츠 품질선)

**템플릿**
```
[무엇] 돈카츠 전문점.
[어디] 도쿄 고토구, 스미요시역 도보 5분.
[영상 맥락] 워뇨타이 추천으로 소개됨.
[실용 정보] 오픈 전 대기 발생. 로스카츠 2,000엔대.
```

| 해야 함 | 하지 말 것 |
|---------|-----------|
| 영상에서 **확인된 사실**만 | 영상 대사 그대로 옮기기 |
| 짧은 불릿 3~5개 | 줄거리 재서술("먼저 ~에 갔다가...") |
| 가격·대기·위치 등 실용 정보 | 우리가 안 가본 곳에 대한 평가·별점 |
| 원어 상호 병기 | 감탄사·과장 카피 |
| 출처 영상·타임스탬프 명시 | 출처 없는 장소 공개 |

**금지어 자동 검사**(어드민): `진짜, 미쳤, 인생, 존맛, 대박, JMT` + 영상 자막과 12자 이상 연속 일치 시 경고.

### 7.4 첫 콘텐츠 배치 계획

#### Batch 0 — 검증용 1조각 (Week 1~2, 코드 최소)

| 항목 | 선정 기준 | 후보 |
|------|-----------|------|
| 채널 | 도시 밀도가 높고 본편(롱폼) 자막이 살아있는 한국어 채널 | 추성훈(실험 성공 이력), 곽튜브, 빠니보틀 중 1 |
| 도시 | 해당 채널이 **영상 2개 이상** 남긴 도시 + 검색량 높은 도시 | 도쿄 / 오사카 / 다낭 |

→ **1채널 × 1도시**, 12핀. 정적 페이지 1장으로 배포해도 무방.
→ 커뮤니티 3곳 공유 → 저장·공유·"다른 채널도" 요청 측정.

#### Batch 1 — MVP (Week 3~8, 목표 12~15조각)

```
채널 4~5개 × 도시 2~3개
├─ 한국어 해외여행 채널 4  (먹방형 2 + 배낭형 2)  ← 톤 다양성
└─ 도시는 겹치게 배치       ← /city/[city] 교차 페이지 성립 조건

도시 겹침 설계 (중요)
  도쿄 : 채널A, 채널B, 채널C   ← 3채널 겹침 = 교차 페이지 킬러
  오사카: 채널A, 채널B
  다낭  : 채널C, 채널D
  방콕  : 채널B, 채널D
```
> 채널마다 다른 도시를 고르면 `/city/*` 페이지가 전부 1채널짜리가 되어 **가장 차별적인 뷰가 사라진다.**
> → **도시를 먼저 3~4개 고정하고, 그 도시에 간 채널을 역으로 선정**하는 순서가 맞다.

#### Batch 2 — 확장 (Week 9~, 데이터 기반)

우선순위 산식으로 다음 조각을 정한다.
```
점수 = 검색요청수(SearchMiss) × 3
     + 해당 도시 기존 페이지 유입 × 2
     + 채널 구독자 규모(로그)
     - 제작 난이도(자막 부실·핀 확정 어려움)
```
- EN UI + 영어권 채널(Kara and Nate 등)은 **한국어 12조각 안정화 이후**.

### 7.5 콘텐츠 운영 리듬 (주간)

| 요일 | 작업 | 시간 |
|------|------|------|
| 평일 저녁 1회 | 조각 1개 STEP 1~4 (추출·확정) | 1h |
| 평일 저녁 1회 | 조각 1개 STEP 5~7 (요약·공개) | 1h |
| 주말 | 조각 1개 통짜 + 지표 확인 + 백로그 정리 | 2~3h |
| **월 1회 (필수)** | **영상 메타 30일 갱신 배치** + 기존 조각 갱신 + 링크 깨짐 점검 | 1h |

**규칙**: 캡 초과 시 **조각 수를 줄이지, 품질 기준(7.1)을 낮추지 않는다.**

### 7.6 콘텐츠 품질 유지

| 리스크 | 대응 |
|--------|------|
| 폐업한 가게 | 분기 1회 Places API `business_status` 배치 확인 → `영업 종료` 뱃지 |
| 영상 삭제·비공개 | 월 1회 videoId 유효성 배치 → 출처 잃은 장소는 **비공개 전환** |
| **오확정(동명이점)** | ★ 최대 구조적 리스크. 지점명까지 확정 못하면 `candidate` 유지(확정 금지) · 확정 근거를 `sourceNote`에 필수 기록 · 장소 상세에 `정보 수정 제안` 링크 · 정정 요청 시 **선비공개 후검토**. LEGAL.md 4.6 |
| **영상 메타 30일 초과 보관** | YouTube API 정책 위반. 월 1회 갱신 배치로 `apiFetchedAt` 리셋 |
| 요약 톤 붕괴 | 조각 공개 전 체크리스트에 "템플릿 4줄 준수" 항목 |
| 삭제 요청 | 접수 → **선차단 후 검토**. 채널 단위 요청 시 해당 채널 전체 비공개 |

---

## 8. 기술 결정 (README 10장 확정 제안)

| 영역 | 결정 | 이유 |
|------|------|------|
| 프레임워크 | **Next.js App Router + SSG/ISR** | SEO 필수, 페이지 수백 개 정적 생성이 정확히 이 용도 |
| DB | **Supabase(Postgres)** | 어드민 인증·스토리지 포함, 사이드 운영비 0 시작 |
| 지도 | **Google Maps JavaScript API** (LEGAL.md 4장 **A안** — 2026-08-04 채택) | 키 발급 완료로 확정. 렌더러가 구글이므로 **Places 데이터 표시가 약관상 허용**됨. 키는 "Maps JavaScript API 하나만 + HTTP 리퍼러" 제한 필수 |
| 장소 좌표 입력 | 어드민 수동 입력 (Batch 0) → 이후 Places 검색 후보 자동화 가능 | A안 채택으로 Places Text Search 를 어드민 후보 검색에 쓰는 길이 열림 (서버 키 별도, 종량 과금 주의) |
| 딥링크용 `place_id` | Google Places API로 확정 시 1회 조회 후 저장 (캐싱 제한 면제 대상) | 지도 앱 열기 링크용 |
| 국내 장소 | Kakao Local | 국내는 카카오맵 딥링크가 UX 우위 |

> **지도 렌더러 결정 이력**: 처음엔 비용 때문에 MapLibre + Overture/Foursquare(B안)를 골랐으나,
> Google Maps 키가 발급되면서 **A안(전면 Google)으로 변경** (2026-08-04). 무료 티어(월 1만 로드/SKU)로
> MVP 트래픽은 충분하고, 약관 제약이 전부 사라진다. 아래 경고 블록은 **B안으로 되돌릴 경우**에만 적용된다.
| 영상 메타 | YouTube Data API (목록·제목·게시일) | 쿼터 내 배치 |
| 자막 | 내부 스크립트, **DB 미저장** | 정책 준수 |
| 호스팅 | Vercel | ISR·이미지 최적화 |
| 분석 | Vercel Analytics 또는 GA4 + Search Console | 콘텐츠 우선순위 결정에 필요 |
| 수익 | AdSense (Phase 1 말 신청) | 정책 페이지 4종 선행 필요 |

**빌드 전략**: 조각 공개 시 해당 채널·도시·장소 페이지만 `revalidatePath`. 전체 재빌드 금지.

### ⚠️ 지도·장소 데이터 약관 제약 (변경 불가)

Google Maps Platform Service Specific Terms:

> Customer will not **(i) display or use Places content on a non-Google Map**, (ii) display Street View imagery and non-Google Maps on the same screen, or (iii) link a Google Map to non-Google Maps Content or a non-Google map.

→ **Google Places에서 받은 상호명·주소·좌표를 MapLibre 지도에 표시하면 약관 위반이다.**

| 허용 | 금지 |
|------|------|
| `place_id` 저장 (무기한, 캐싱 제한 면제) | Places 상호·주소·좌표를 비구글 지도에 표시 |
| Google Maps 딥링크 (아웃링크) | Places 데이터로 자체 지도 구성 |
| 오픈 데이터셋 좌표를 MapLibre에 표시 | Google Maps와 타 지도 동시 노출 |

**설계 규칙**: 지도에 찍는 데이터와 딥링크용 `place_id`를 **DB 필드 수준에서 분리**한다.
`Place.lat/lng/name` ← 오픈 데이터셋 / `Place.googlePlaceId` ← 딥링크 전용.

**미검증**: 도쿄·방콕 골목 식당의 오픈 데이터셋 매칭률. Batch 0(12핀)에서 검증하고, 매칭률이 낮으면 Google Maps JS API + Places 전면 채택으로 전환(비용 발생).

상세: `LEGAL.md` 4장

---

## 9. SEO / AdSense 실행 계획

### 9.1 타깃 쿼리 3층

| 층 | 예시 | 착지 페이지 |
|----|------|-------------|
| 헤드 | `여행 유튜버 맛집 지도` | `/` |
| 미드 ★ | `곽튜브 도쿄 맛집`, `빠니보틀 다낭` | `/c/[creator]/[city]` |
| 롱테일 | `마루시치 돈카츠 곽튜브`, `도쿄 유튜버 간 곳` | 장소 상세 / `/city/*` |

**미드 쿼리에 자원의 80%.** 채널명+도시명은 경쟁이 거의 없고 의도가 명확하다.

### 9.2 필수 실행 항목

- [ ] `sitemap.xml` 자동 생성 (조각 공개 시 갱신), `robots.txt`
- [ ] 전 페이지 `canonical` 명시 (특히 `/place/*` ↔ `/c/*/*/*` 중복 방지)
- [ ] 구조화 데이터: `WebSite` / `ProfilePage` / `ItemList` / `Place` / `BreadcrumbList`
- [ ] OG 이미지 자동 생성 (`@vercel/og`) — 지도 배경 + 채널명 + 도시명 + 핀 수
- [ ] 내부 링크 최소 3방향(상위/형제/교차)을 모든 페이지에
- [ ] Search Console 등록 + 조각 공개 시 URL 검사 요청

### 9.3 AdSense 승인 조건 역산

| 조건 | 대응 |
|------|------|
| 고유·유용한 콘텐츠 | 자체 작성 요약 + 지도 데이터 (스크래핑 나열 X) |
| 충분한 분량 | **조각 12개 이상** 확보 후 신청 (≈ 페이지 100+) |
| 정책 페이지 | `/about` `/privacy` `/policy` `/takedown` 선행 |
| 저작권 | 자막 원문·영상 임베드 대량 게시 회피, 썸네일은 공식 API URL 사용 |
| 광고 배치 | 지도·핀 근처 금지. 리스트 중간 1개 + 상세 하단 1개 |

> 신청 시점: **Phase 1 종료 직후**. 조각 3~4개 상태로 신청하면 반려 확률이 높고 재신청 비용이 크다.

---

## 10. 로드맵 & 지표

| 단계 | 기간 | 만드는 것 | 콘텐츠 | Go 판단 |
|------|------|-----------|--------|---------|
| **P0 검증** | W1~2 | 정적 1페이지(지도+리스트) | 1채널×1도시 12핀 | 공유 후 저장/재방문/"다른 채널도" 요청 발생 |
| **P1 MVP** | W3~8 | 홈·크리에이터·채널×도시·장소상세·검색·정책4종 + 어드민 최소 | 4~5채널 × 12~15조각 | 자연 검색 유입 발생, 조각당 유입 우상향 |
| **P1.5** | W9~10 | `/city/*` 교차 페이지, OG 자동생성, AdSense 신청 | 도시 겹침 보강 | AdSense 승인 |
| **P2** | W11~ | 즐겨찾기(localStorage), EN UI, 필터 고도화 | 영어권 채널 진입 | 월 유입 목표 달성 시 |
| **P3** | — | 앱/앱인토스, 제휴 | — | 웹에서 검증된 후에만 |

### 지표 (측정 안 하면 조각 우선순위를 못 정한다)

| 단계 | 핵심 지표 |
|------|-----------|
| P0 | 공유수, 재방문율, 요청 발생 |
| P1 | 조각당 월 세션, **아웃링크 CTR**(영상/지도앱), 검색 유입 비중 |
| P1.5 | 색인된 페이지 수, 미드 쿼리 노출·순위 |
| P2 | 세션당 페이지뷰(교차 탐색이 작동하는지), AdSense RPM |

**북극성 지표: `조각당 월 아웃링크 클릭수`**
페이지뷰가 아니다. 사람들이 실제로 영상·지도로 넘어가야 이 제품이 일한 것이다.

---

## 11. 이 문서에서 내린 결정 요약 (README 16장 "열린 결정" 대응)

> ⚠️ **법적·약관 검토 결과는 `LEGAL.md`에 별도 정리했다.** 아래 표의 지도/장소 데이터 항목은 그 검토 결과로 이미 한 차례 수정된 것이다.

| 항목 | 이 문서의 제안 |
|------|----------------|
| 홈 구조 | 세계지도 X → **크리에이터 그리드** |
| 핵심 페이지 | `/c/[creator]/[city]` 단일 허브. 모든 자원 집중 |
| 콘텐츠 최소 단위 | 채널×도시 1조각, **확정 8핀 미만 공개 금지** |
| 도시 선정 순서 | 채널 먼저 X → **도시 3~4개 고정 후 채널 역선정** (교차 페이지 성립) |
| 지도 | MapLibre + 무료 타일 (Mapbox 요금 회피) |
| **장소 데이터** | **Overture / Foursquare OS Places** — Google Places를 비구글 지도에 쓰면 약관 위반 (LEGAL.md 4장) |
| **크리에이터 이미지** | **이니셜 뱃지** — 프로필 사진 미사용 (부경법 §2①타, LEGAL.md 1.3) |
| **영상 메타** | 30일 갱신 배치 필수 (YouTube API §III.E.4.d, LEGAL.md 4.5) |
| AdSense 신청 | 조각 12개 이후 |
| 계정/로그인 | Phase 2까지 **없음** (localStorage로 충분) |
| 다크모드 | Phase 2 |

### 아직 남은 결정 (사람이 정해야 함)

- [ ] 첫 채널 1개 + 첫 도시 1개 (Batch 0) — **다음 액션의 시작점**
- [ ] 도시 3~4개 고정 리스트 (Batch 1 겹침 설계)
- [ ] Google Places 키·결제 계정 개설
- [ ] 브랜드명 Tripin 확정 및 도메인
- [ ] 히든스팟과 레포·인프라 분리 여부 (권장: 분리)

---

*이 문서는 구현 착수 전 합의용이다. 화면·기능은 P0 검증 결과에 따라 조정될 수 있으며, 콘텐츠 정책(7.3)과 법적 안전선(README 7장)은 조정 대상이 아니다.*
